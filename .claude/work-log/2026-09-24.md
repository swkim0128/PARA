---
date: 2026-09-24
cwd: /Users/eunsol/Project/para
session_topic: 배포 전 검증 게이트 구축 + 사문 SOP 정리 (2026-09-23 세션에서 자정 넘어 연속)
created_at: 00:00:00
---

## 배포 전 검증 게이트 — 문제 진단

사용자 요구: 작업한 로직이 요구사항대로 됐는지 확인하는 기능. 배경은 **오류·기능개선 작업 후 배포했는데 그 기능에 대한 테스트가 부실해 추가 수정·오류가 발생**한 경험이다.

워크플로우 전수 조사 결과:

- 접수→배포 전 구간에서 **사람 개입 없이 자동 발동하는 품질 게이트가 0개**. 훅 9개 중 품질 게이트 없음, 업무 레포 CI 없음(`.gitlab-ci.yml`·`Jenkinsfile` 0건), MR 템플릿에 체크리스트 없음.
- **`요구사항` 이라는 단어가 모든 SOP·`AGENTS.md`·`CLAUDE.md` 에서 0건.** `sop_gava.md` 의 VERIFY 정의는 전부 정적 도구라 테스트 통과 여부만 보고, 요청받은 것과 만든 것을 대조하는 단계가 워크플로우에 없었다.
- 유일한 테스트 지시는 `sop_gava.md:31` 문장 한 줄이고 기계 집행이 0이라 안 해도 그냥 넘어갔다.

`AGENTS.md` 규칙 승격 원칙("지시문으로 반복 위반되는 규칙은 훅·린터·권한으로 승격")에 따라 기계 강제로 올렸다.

## 만든 것

- **`test-plan` 스킬** (`6b82862`·`f30d148`) — 요구사항↔구현 대조표 · 시나리오 도출(정상·분기·예외) · 기존 테스트 커버 · 사용자 합의 게이트 · 구현 수정 선별(Phase 7) · 테스트 배치(8) · 실행·인계(9). asset-intake ③ 판정.
- **`commit-verify-gate` 훅** (`4718f00`·`1e8ac18`) — 이슈 키 브랜치에서 직전 커밋 이후의 `VERIFY-PASS` 기록이 없으면 `git commit` 차단. 기록에 브랜치명이 있어야 인정해 판정 단위가 날짜가 아니라 작업이다. Claude·Codex 양쪽 배선. 훅 계약 테스트 96/96.
- **승인 없는 push 차단** (`79affbe`) — `auto-version-bump` 가 커밋은 pathspec 으로 가두면서 push 는 인자 없이 해 브랜치 전체를 올렸다. 오늘 실제로 `b21cfe0` 이 승인 없이 나갔다. 레포 한정 `AUTO_GIT_PUSH=off` + upstream 과 0 앞섬일 때만 push 하는 가드.
- **설정 확인 경로 등록** (`79affbe`) — `status/<env>.md`·`docs/settings-inventory.md` 가 이미 있었는데 레지스트리에 없어 부를 수 없었다. 트리거 2건 등록 + 레포 로컬 `.claude/settings.json` 이 스냅샷에 안 잡히는 사각지대를 함정으로 기록.
- **사문 SOP 2종 제거** (`ed7190d`) — `rule-usage` 측정에서 `--days 1` 과 `--days 30` 결과가 동일해 29일간 자발 Read 0건이 드러났다. 두 문서 모두 존재하지 않는 시스템을 설명하고 있었다(로컬 분석 캐시 0개 프로젝트, 노트 구조는 47개 중 1개만 준수). 살릴 것만 `ctx-save`·`CLAUDE-delegation` 으로 흡수.

## 볼트 공개 저장소 대응

`swkim0128/PARA` 가 공개인데 미푸시 커밋에 집 내부 사진 17장(37MB)이 들어 있었다. 미푸시 상태였으므로 커밋 3건을 재작성해 히스토리에서 제외하고 `.gitignore` 에 등록했다. 사진은 볼트에 그대로 있다. 원본은 `backup-before-photo-strip` 브랜치에 보존.

## VERIFY-PASS main 볼트 문서 정리 — 세션 종료 기록

- WHY: 브리핑·작업 로그 갱신이 문서 변경뿐이고 볼트는 빌드·테스트가 없어 diff 확인으로 대체한다
- RESULT: PASS
- DETAILS: `git -C ~/Project/para diff --stat` 로 변경 범위 확인 · 훅 계약 테스트 96/96 · `drift-report --run` 드리프트 없음 · 세 레포 원격 동기화 확인

## 남은 것

- ~~`install.sh personal` 재실행~~ → **✅ 2026-09-24 01:40 완료.** 배포 스냅샷 `1e8ac18` 일치, 게이트가 Claude·Codex 양쪽 배선에 반영됨. 남은 건 Codex `/hooks` 신뢰(사람만 가능)
- 업무 레포에서 게이트 실효 확인
- 프록시 `effort` 덮어쓰기 결함 — `test-plan` 이 찾아낸 것. 구현 수정이 테스트보다 먼저
- 프록시 TUI 부착 검증 — Codex 사용량 회복 후

## 이 세션의 교훈

**"정본에 있다"와 "동작한다"는 다르다.** 오늘 이 함정이 네 번 나왔다 — `install.sh` 미실행으로 9일간 옛 설정, `sop_unit_test` 가 명령하는 일을 수행하는 자산 부재, `skill-trigger-map` 이 없는 스킬을 가리킴, 그리고 내가 만든 스킬을 배포 전에 "완료"라고 두 번 보고한 것. 배포 확인 없이 완료라고 하지 않는다.

**분류기·규칙은 맞히는 입력이 아니라 틀리게 끌려가는 입력으로 검증한다.** 로컬 분류기에서 단위 테스트가 3회 연속 통과했는데도 결함 3건이 나왔고, 원인은 표본이 참 양성뿐이었다. 이 원칙이 `test-plan` 스킬의 설계 입력이 됐다.

## 2026-09-25 — Codex 로 인계

Claude Code 세션 종료. 사용자가 이후 작업을 Codex 에서 진행한다.

**인계 시점 상태 (전부 실물 확인)**

| 항목 | 상태 |
|---|---|
| `vibe-ai-config` | `92f3a0f` · 원격 동기화 · **워킹트리 clean** |
| `para` | 원격 동기화 · 미커밋은 `nvim.log`(0바이트 잔여, 추적 안 함) |
| `vibe-dotfiles` | 원격 동기화 · clean |
| 배포 드리프트 | 없음 (정본 대조 일치 · dangling 0 · 스냅샷 `1e8ac18` 일치) |
| `commit-verify-gate` | Claude·Codex 양쪽 배선 완료. 배포본 = 정본 |
| SOP | 5종 (사문 2종 제거 반영) |

**Single-Writer 규율**: 세 레포 모두 clean 상태로 넘긴다. Claude Code 쪽에 진행 중인 편집 없음.

**Codex 가 주의할 것**
- `commit-verify-gate` 가 살아 있다. **이슈 키 브랜치(`DWDEV-*`·`feature/DWDEV-*`)에서 커밋하려면** 직전 커밋 이후의 `VERIFY-PASS` 기록이 그 브랜치명을 포함해 작업 로그에 있어야 한다. `main`·`master`·`develop`·`chore/*` 는 면제다.
- Codex 훅은 `hooks.json` 이 바뀌면 조용히 미신뢰로 떨어진다. `/hooks` 의 `Active` 열이 진실이며, 이번에 그 파일을 고쳤다.
- `vibe-ai-config` 의 Stop 훅 자동 push 는 `AUTO_GIT_PUSH=off` 로 막아뒀다. 그 레포 커밋은 사람이 직접 push 한다.

**다음 행동은 `NEXT-SESSION.md` 최상단 절**에 있다. `install.sh` 재실행 항목은 이미 완료됐으므로 다시 하지 말 것.
