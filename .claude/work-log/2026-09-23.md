# 작업 로그 — 2026-09-23

## 노션 프로젝트 확인 및 PARA 동기화

- **요청**: 웹 리서치 자동화 엔진 테스트 전, 진행해야 할 개인 노션 프로젝트를 확인하고 PARA에 동기화.
- **조회 범위**: Notion Projects DB의 `개인` 태그 미완료 프로젝트와 관련 Tasks DB. 읽기 전용 조회.
- **확인 결과**:
  - `집 정리`·`벌크업`·`2026 지출 예산 및 가용 금액 계획`은 `In progress / 높음`.
  - `대구 본가 4박 5일`은 `Planning / 중간 / 2026-09-26 종료 예정`.
  - `웹 리서치 자동화 엔진(PRO-132)`은 `Planning / 중간`이며 열린 태스크 행은 조회되지 않음.
  - `집 정리`와 `벌크업`의 일부 높은 우선순위 태스크는 기한이 경과한 상태로 남아 있음.
- **판단**: 노션 상태·우선순위·태스크를 추정 변경하지 않는다. 웹 리서치 엔진 테스트는 프로젝트 확인 완료 후 재개한다.
- **다음 행동**: 쿠팡 쓰기 테스트 결과 확인 → recipe 검증·원복 필드 확정 → 실행기 TODO(YAML→JSON 빌드, `buildReplScript`) 처리.
- **동기화 파일**: `NEXT-SESSION.md` 최신 착수 지점에 동일 내용을 반영.

## 진행 상태 보정 — 집 정리·블랙프라이데이 구매

- 사용자 확인: 집 정리의 바닥 정리는 완료했고, 책상·부엌 짐 정리가 남아 있다. 블랙프라이데이 구매 품목 정리를 진행한다.
- Notion 반영:
  - `1회차 — 바닥 정리` → `Done`.
  - `바닥·책상 정리 실행 (Step 0~4)` → `책상·부엌 짐 정리 실행`, `In progress`. 다음 한 걸음을 책상·부엌 분류로 갱신.
  - `2026 예정 지출 품목·가용 금액 정리` 태스크 → `In progress / 높음`, PRO-131 연결.
- 사용자 정정 반영: 프로젝트 목적은 블랙프라이데이 구매 자체가 아니라 예정 지출의 예산 산정과 가용 금액 확인이다. Notion 프로젝트 제목을 `2026 지출 예산 및 가용 금액 계획`으로 변경하고 설명·목표 문단도 예산 중심으로 갱신했다.
- 추가 정정: 연간 범위로 보이지 않도록 제목을 `2026 하반기 지출 예산 및 가용 금액 계획`으로 변경했다.

## 11월까지 자산관리 계획 반영

- 프로젝트 설명과 연결 태스크를 9~11월 수입금·예정 지출·가용 금액 산정 중심으로 갱신했다.
- 노션 본문에 월별 수입 확정, 품목별 지출 상한, 목적저축 100만원 우선 확보, 비상금 344만원 분리, 11월 말 대조 절차를 추가했다.
- 확정 전 값: 9~11월 실수령 수입, 9월 Household Ledger 2,730,000원의 성격, 품목별 최종 결제액, 고정생활비 합계.
- PARA `NEXT-SESSION.md`의 최신 착수 지점과 프로젝트 우선순위 표를 갱신했다.

## 수입 기준 확정 및 예정 지출 목록 정리

- 사용자 확인: 월 수입금은 동일한 2,845,000원.
- 9~11월 총수입 기준: 8,535,000원.
- 노션 프로젝트에 예정 품목 목록과 금액 입력 대기 상태를 추가했다. 다음은 품목별 최종 결제액·결제 예정월·구매 상한액 확정이다.

## 이번 달 실제 지출 및 치아교정 반영

- 사용자 확인 지출: PT 330,000원, 레이저 제모 358,000원, 치아교정 검증 100,000원.
- 이번 달 실지출 합계: **788,000원**.
- 기존 Ledger/예산 자료의 치아교정 치료 예정액 약 5,300,000원은 향후 치료비로 확인했다. 검증 비용 100,000원과 중복 계산하지 않도록 분리했다.
- 노션 프로젝트와 연결 태스크에 실제 지출·치아교정 분리 규칙을 반영했다.
- 사용자 정정 반영: 치아교정 검증 비용 100,000원은 총 예상비용 5,300,000원에 포함된다. 향후 잔여 치료비는 약 5,200,000원으로 계산하고, 위시리스트·예산 페이지·프로젝트의 중복 계산을 제거했다.

## 최신 월 예산 구조 반영

- 월 수입 2,845,000원 중 월 운영 예산은 1,630,000원으로 고정지출·변동지출을 포함한다.
- 일반 저축은 300,000원·150,000원·100,000원으로 월 550,000원이다.
- 블랙프라이데이 목적자금 1,000,000원은 11월 16일까지 별도 확보한다.
- 예정 품목과 큰 지출은 월 운영 예산 외 지출로 판정하고, 월 저축·목적자금·비상금/투자금 보존 후 구매 가능 여부를 품목별로 확인하도록 프로젝트와 위시리스트에 저장했다.

## 11월까지 지출 예정 정리

- 월 운영예산: 월 1,630,000원, 9~11월 합계 4,890,000원.
- 예산 외 확정·계획 항목: 축의금 700,000원, 치아교정 잔여 약 5,200,000원, 블랙프라이데이 1,000,000원 한도 → 약 6,900,000원.
- 리프트업 수납침대·인덕션·에어프라이어·수면안대·의류는 금액 미정으로 합계에서 제외하고 구매 가능성 판정 대상으로 남겼다.

## 10월 1일 교정치과 일정 반영

- 10월 1일 교정치과 방문 및 일부 교정비 결제 가능성을 프로젝트에 추가했다.
- 금액은 미정으로 유지하고, 당일 결제액·잔여금·분할납부 회차·다음 결제일 확인 태스크(TAS 신규)를 생성했다.

## 작업별 모델 선택·Jev 연결

- 요청: 작업 요청 후 상황에 맞는 모델 자동 선택, 사전 작업별 기본 매핑, Jev 활용.
- 기존 프로필을 재사용해 `vibe-ai-config`에 `vibe run` 실행기·작업 매핑·Jev 선택적 연동 구현. 외부 저장소는 격리 에이전트가 승인된 복사로 정본 반영했다.
- 모델 정책: 단순 작업 Luna/low, 일반 구현 Terra/medium, 리뷰 Sol/medium, 복잡 설계 Astra/high. 예산·프로젝트 관리 전체를 경량 모델로 고정하지 않는다.
- 메인 재검증: 실제 typesafe-sdk 0.7.0 모의 HTTP 포함 8/8 PASS, launcher dry-run에서 Sol/medium 실행 인자 확인, `bash -n`·`git diff --check` PASS.
- 지원 경계: 작업 시작 시 선택만 구현. 현재 TUI 매 입력 모델 자동 전환은 미구현. Jev 환경변수 미설정으로 live 호출은 하지 않았으며 실사용 절감 효과도 미검증.
- 커밋·푸시·전체 install 없음. 상세 및 다음 행동은 [[작업별-모델-라우팅-20260923]]·`NEXT-SESSION.md`에 기록했다.

## 코덱스 → 클로드 인계 후 — 작업별 모델 라우팅 이어받기

- 배경: 사용량 한계로 Codex 에서 전환. 브리핑 확인 후 `vibe run` 라우터 산출물이 `vibe-ai-config` 에 미커밋 상태로 남아 있는 것을 확인하고 재검증했다(테스트 통과, dry-run 정상, 모델 캐시 4종 존재).
- **Jev 무기한 대기 확정**: typesafe.ai 가입·로그인이 막혀 키 발급 불가. 코드는 키 부재 시 안전 강등되므로 **제거하지 않고 휴면 존치**하고, 로컬 규칙 분류를 주 경로로 승격했다. 실호출 검증 하네스는 준비해 뒀다.
- **매 입력 자동 라우팅 실현가능성 확인** — 기존 노트의 판단 3건을 정정했다. `turn/settings/update` 가 진행 중 턴의 모델을 바꾸고, `codex --remote` 로 공식 TUI 를 외부 app-server 에 붙일 수 있어 **TUI 재작성 없이 프록시 미들웨어로 가능**하다. `model/list` 라이브 조회도 실호출 성공.
- **로컬 분류기 강화** — `intent_rules` 3건 → 9건, `implement_guard` 신설, `predefined-command` 오탐 수정, `--verify-models-live` 옵트인 추가. 격리 에이전트에 위임하고 **적대적 검증으로 결함 3건(과매칭·과소매칭·복합요청)을 잡아 4차에 마무리**했다. 최종: 단위 18/18 + 하네스 3종 전부 통과.
- 상세·판정 로직·검증법 교훈은 [[작업별-모델-라우팅-20260923]] 에 기재.
- **미처리**: `vibe-ai-config` 미커밋 8건 · `vibe-dotfiles` 미커밋 10건 커밋·배포 판단. `--verify-models-live` 의 문서·래퍼 노출. 프록시 구현 착수.
