# 2026-09-19 작업 로그

## Instapaper MCP 설정 마무리 (vibe-ai-config) — 완료

Codex 가 오전에 진행하다 사용량 한도로 중단한 작업을 Claude Code 로 이어받아 완주했다.
메인은 para cwd, 실제 레포 작업은 `%10` pane 위임(vibe-ai-config, Claude).

### Codex 가 남긴 지점 (오늘 07:30~08:30)

- 신규: `codex/instapaper-mcp.sh`(Keychain 래퍼) · `codex/instapaper-mcp.toml`(관리 구획 템플릿) · `docs/instapaper-mcp.md`
- 수정: `install.sh`(심링크 + config 관리 구획 4번째 + 검증기 인자 확장) · `shared/mcp.base.json`(codexOnly.instapaper) · `codex/README.md` · `codex/config-root.toml` · `status/personal.md`
- MCP 본체: `hendronf/Instapaper-MCP` `V1.0.1` → `~/Library/Application Support/vibe-ai-config/instapaper-mcp` 클론·빌드 완료
- 중단 시점: `instapaper-mcp.toml` 의 `enabled` 를 따옴표로 감싸고 `install.sh` 의 sed 를 따옴표째 치환하도록 고친 직후 (배포·검증 미실행)

### 이번에 한 것

1. **래퍼 결함 수정** — `codex/instapaper-mcp.sh` SC2155. `set -euo pipefail` 아래에서 `export VAR="$(...)"` 는 종료코드가 `export` 에 가려져, Keychain 항목이 없어도 **빈 값으로 node 가 기동**됐다. 대입/선언을 분리하고, 항목이 없으면 사유 1줄 + 등록 명령을 stderr 에 내고 `exit 1`.
2. **배포** — `./install.sh personal` (기본 `INSTAPAPER_MCP_ENABLED=false`).
3. **실동작 테스트** — 더미 자격증명으로 MCP 본체를 stdio 로 띄워 JSON-RPC 핸드셰이크 실측.

### VERIFY (실측)

| 항목 | 결과 |
|---|---|
| `bash -n` · `shellcheck -S warning` (래퍼) | 통과 (SC2155 4건 소멸) |
| `~/.codex/instapaper-mcp.sh` | 레포 파일로 심링크됨 |
| `~/.codex/config.toml` 관리 구획 | `command = ~/.codex/instapaper-mcp.sh` · `enabled = false` |
| tomllib 타입 검사 | `enabled` = bool `False` (문자열 아님) |
| `codex mcp list` | exit 0 — `instapaper … disabled` |
| `install.sh personal` 재실행 | config 전문 diff 없음 (멱등) |
| `INSTAPAPER_MCP_ENABLED=true` 렌더링 | `enabled = true` 불리언 (임시 파일 재현, 실제 활성화 안 함) |
| 래퍼 가드 | 없는 계정으로 동일 경로 태움 → 사유 + 등록 명령 출력 후 exit 1 |
| MCP 핸드셰이크 (더미 자격증명) | `initialize` OK (`instapaper-mcp-server 1.0.0`) · `tools/list` **26개** |

### 발견 — 인증 실패해도 서버가 죽지 않는다

`build/index.js` 는 env 4개를 **존재 여부만** 검사한 뒤 기동 즉시 인증을 시도하는데,
실패해도 프로세스를 끝내지 않고 계속 서비스한다 (stderr 에 `Failed to authenticate: Unauthorized` 만 찍음).
→ API 승인 전에 `enabled = true` 를 켜면 **붙어는 있는데 전 호출이 실패하는** MCP 가 된다.
기본 비활성 배포가 옳다는 근거. 활성화 전 인증 확인 명령을 `docs/instapaper-mcp.md` 에 추가했다:
`~/.codex/instapaper-mcp.sh < /dev/null` (이 맥에는 `timeout`·`gtimeout` 이 없어 stdin 을 닫아 자기 종료시키는 형태로 확정).

### 남은 사용자 단계

1. Instapaper 개발자 API 승인 (https://www.instapaper.com/api) — consumer key·secret 발급
2. Keychain 4개 항목 등록 (`docs/instapaper-mcp.md` 의 `security add-generic-password` 4줄) — **현재 전부 미등록**
3. `~/.codex/instapaper-mcp.sh < /dev/null` 로 인증 확인
4. `INSTAPAPER_MCP_ENABLED=true ./install.sh personal` → Codex 재시작

### 미결

- vibe-ai-config **미커밋 그대로** (커밋·푸시 안 함). 기존 Phase 1·2·3 변경과 Instapaper 변경이 한 워킹트리에 섞여 있다.
- 위임 pane `%10` 열려 있음.
- 위임 중 겪은 것: `vibe delegate` 메시지 주입이 Enter 를 누르지 않아 프롬프트에 머물렀고(수동 `send-keys Enter` 로 제출), 이후 API 재시도·인터럽트로 한 번 더 끊겼다. `pane-wait.sh` 는 훅 실행 중 상태를 유휴로 오판해 조기 반환했다.

---

## Archify · Jev 도입 — 조사 + 계획 수립 (실행 전)

사용자가 "소스코드 분석해서 그림으로 보여주는 도구" 를 찾는 데서 출발해, 후보 조사 → Archify 특정 → Jev 추가 조사 → 계획서 작성까지.
**실행은 Codex 에서 먼저 한다**(사용자 결정 9/19). 이 세션에서는 아무것도 설치하지 않았다.

### 조사 경로

1. 1차 후보군 조사 — CodeBoarding · GitDiagram · DeepWiki · repowise · CodeSee · Serena MCP · madge/dependency-cruiser
   - 업무컴 제약(회사 코드 외부 전송 불가) + 업무 스택(Kotlin·PHP) 두 축으로 거의 다 탈락
   - **CodeBoarding 은 이 맥에서 실행 불가** — 정적 분석은 로컬이지만 구조 요약에 LLM 공급자 필요. 실측: `ollama` 없음 · `ANTHROPIC_API_KEY`·`OPENAI_API_KEY`·`GEMINI_API_KEY` 전부 없음. 게다가 **Kotlin 미지원**
2. 사용자가 **Archify** 를 지목 → 저장소 1차 조사
3. 사용자가 wikidocs 글(@yuncastle, 9/17)로 **Jev** 제시 → 공식 문서·SDK 레지스트리로 교차 검증
   - wikidocs 는 Cloudflare 챌린지라 curl 403 → **chrome-devtools MCP 로 실제 브라우저 렌더 후 본문 추출**

### 확정한 사실 (1차 출처)

**Archify** — `tt-a1i/archify` MIT ★66.8k
- 스킬 실체는 레포 루트가 아니라 `archify/` 하위. **런타임 의존성 0**(devDependencies 뿐) → 순정 Node ≥18
- 기본 브랜치 = 개발판 `2.17.0-dev.1` / 안정 태그 = **`v2.16.0`(8/30)** → 벤더링은 태그 기준
- 흐름: `guide` → 스키마·예제 1개씩 → 후보 JSON → `validate --quality showcase`(9체크·0error·0warning) → `deliver`(SHA-256 영수증) → `visual-check`
- 외부 통신 = 업데이트 확인 1건. **`ARCHIFY_UPDATE_CHECK_DISABLED=1` 이면 즉시 silent**(`check-update.mjs:1637` 원문)
- `visual-check` 는 **로컬 Chrome 을 CDP 파이프로 직접 구동**(`bin/visual-check.mjs:107-130`). 번들 브라우저 없음, `ARCHIFY_CHROME` 로 경로 지정
- 🔴 **검증기는 렌더링 정합성만 본다.** 코드에 없는 의존성을 그려도 통과한다 — 소개 글들의 "없는 의존성 검증" 은 과장

**Jev** — TypeSafe AI, 2026-09-15 공개
- SDK 실재: PyPI `typesafe-sdk` 0.7.0(9/18) · npm `@typesafe-ai/sdk` 0.6.0
- `jev-latest` → `jev-1.13.0` · 64k/요청, state 32k · Choice 255개 · Score 2~10 · 250k tok/s, 1200 req/min
- 키: console.typesafe.ai/settings/keys (문서엔 waitlist 언급 없음, 런칭 글은 early access — 실제 발급 여부 미확인)
- 70~500ms 는 **TypeSafe 자체 발표 + 미 서부 노트북 측정** → 한국 기준 아님

### 산출물

- 계획서 [[01.Projects/개인컴_AI_작업환경_업그레이드/archify-jev-도입계획-20260919]] — Phase A(격리 테스트) → B(정본 벤더링·배포) → C(Jev). A·B 와 C 는 병렬
- 중단 게이트 2개: **A-3** 엣지 대조(근거 없는 엣지 0건) · **C-4** 골든셋 정확도(현행 Opus 미만이면 편입 안 함)
- Jev 도입 유보 3건(볼륨·외부 전송·감사 추적)은 계획서 §2-D 에 기록하고 전부 검증 게이트로 전환. 업무컴 적용(C-6)은 회사 정책 게이트로 분리

### 다음

Codex 세션에서 **A-1**(v2.16.0 격리 클론)부터. 사용자 단계는 **C-1**(TypeSafe 키 발급 시도) — Phase A 와 병렬이라 선후 없음.

---

## Archify · Jev 도입 — Phase A-1~A-4 실행

## 16:00:00 — WRITE Archify 격리 검증 산출물
- WHY: 확정된 도입 계획의 A-1~A-4를 정본·스킬 경로 변경 없이 검증한다.
- RESULT: PASS
- DETAILS: `/private/tmp/archify-v2.16.0`에 `v2.16.0` 태그를 shallow clone했다. `git describe --tags`=`v2.16.0`, CLI help exit 0, 벤더 후보 `archify/` 크기 7.3MB. Instapaper MCP 아키텍처 JSON·A-3 엣지 근거표·HTML을 모두 스크래치패드에만 생성했다.

## 16:00:00 — VERIFY-PASS Archify A-2~A-4
- WHY: 첫 다이어그램의 렌더링 품질·소스 근거·브라우저 결과를 계획 게이트로 확인한다.
- RESULT: PASS
- DETAILS: `validate --quality showcase --repo-root`가 artifact 9/9, composition error 0, warning 0을 보고했다. 관계 5건 모두 소스 줄 근거가 있으며 근거 없는 엣지 0건(A-3 통과). `deliver` 영수증: spec SHA-256 `6aacd9b...c072c7`(3,391B), HTML SHA-256 `701656...68003d`(707,998B). 권한 경계 밖 local Chrome `visual-check`은 4개 viewport와 light/dark capture에서 containment·readability 통과. 샌드박스 내 Chrome SIGABRT는 권한 경계 밖 재실행으로 해소됐다.

### 다음

- A-5: dirty 상태의 `vibe-ai-config`는 읽기 전용으로 조사했으며, `install.sh` 배포 흐름의 10단계 workflow 검증을 스크래치패드에서 수행한다.
- Phase B 정본 편입은 A-5 완료 후 별도 격리/위임으로만 진행한다. Jev C-1은 여전히 사용자 키 발급 단계다.

---

## Jev 개념 자료 보강

## 16:20:00 — WRITE Jev 참고 노트 보강
- WHY: Jev가 Claude Code·Codex를 대체하는지에 대한 개념 혼동을 외부 자료로 확인하고, 개인 지식 노트에 구분을 남긴다.
- RESULT: PASS
- DETAILS: [[03.Resources/21.Tools/Jev - 정형 판단 모델 개념과 활용]]에 두 번째 블로그의 "글을 쓰지 않는 AI" 관점을 추가했다. Jev는 Choice·Score·Noul의 타입화된 확률 판단을 반환하지만 파일·도구·명령을 직접 다루지 않는 모델이며, Claude Code·Codex 또는 애플리케이션 코드가 그 판단을 받아 실제 작업을 수행한다는 역할 분리를 명시했다.

## 16:20:00 — VERIFY-PASS 문서 변경 정합성
- WHY: 한국어 파일명·공백을 포함한 Jev 노트 변경에 공백 오류가 없는지 확인한다.
- RESULT: PASS
- DETAILS: `git diff --check -- "03.Resources/21.Tools/Jev - 정형 판단 모델 개념과 활용.md"` exit 0.

---

## Archify · Jev 도입 — 개인 전용 Jev 정책 인계

## 16:35:00 — CONTEXT-SAVE 정책 결정과 진행 중 위임 기록
- WHY: 사용량 한도 전에 Jev의 개인 전용 사용 결정을 다음 세션에 안전하게 인계한다.
- RESULT: SAVED
- DETAILS: 사용자는 Jev를 개인 데이터 전용으로 분리하고 업무 설정에는 연결하지 않기로 결정했다. `vibe-ai-config` 격리 worktree `jev-personal`의 pane `%11`에서 개인 프로필 기본 비활성화·Keychain 기반 `vibe-jev` 래퍼·개인 사용 정책 문서를 구현 중이다. 마지막 관찰 시 래퍼 shellcheck 보정 뒤 최종 검증 단계였다. Archify는 이미 별도 `chore-archify` worktree에서 개인 설치까지 실행되어 전역 스킬 심링크가 해당 미커밋 worktree를 가리킨다.

## 16:35:00 — NEXT-SESSION 갱신
- WHY: 다음 세션의 첫 행동과 미완 위임을 SoT에 남긴다.
- RESULT: SAVED
- DETAILS: `NEXT-SESSION.md`의 Archify·Jev 최상단 항목을 배포·개인 전용 정책·pane `%11` 재개 절차·검증 할 일로 갱신했다.

---

## Jev 개인 전용 분리 완주 (Codex 한도 소진 → Claude Code 이어받음)

## 23:55:00 — CHECK Codex pane `%11` 중단 지점 확인
- WHY: Codex가 사용량 한도로 멈춘 위치와 남은 산출물의 온전성을 판정한다.
- RESULT: PASS
- DETAILS: pane `%11` 은 `Failed to apply patch` + `5h 한도 4% 남음` 으로 중단. 실패한 패치는 워크트리 작업 로그 파일 생성이었고(`.claude/work-log/` 디렉터리만 생성, 파일 0건), 코드 변경 4건은 워킹트리에 온전히 남아 있었다. 부수 확인: `vibe-ai-config` master 가 `c99a80d` → `3229576` 으로 커밋·푸시되어 Phase 1·2·3 47개 미커밋이 해소됐고, 남은 미커밋은 Instapaper 9건뿐이다.

## 23:58:00 — VERIFY-PASS Jev 개인 전용 자산 재검증
- WHY: Codex의 자체 보고를 신뢰하지 않고 정적 도구로 직접 입증한다.
- RESULT: PASS
- DETAILS: `bash -n install.sh` 통과 · `shellcheck shared/scripts/jev-personal.sh` 경고 0 · 배선 순서 확인(프로필 source L74 → `INSTALL_LINKS` 선언 L127 → Jev 분기 L163) · `ls ~/.local/bin/vibe-jev` exit 1 로 기본 비활성(`JEV_ENABLED=0`) 실증 · `profiles/work.conf` 무변경으로 업무 프로필 무오염 확인.

## 2026-09-20 00:05 — COMMIT Jev 개인 전용 분리
- WHY: 검증이 끝난 변경을 Archify·Instapaper 작업과 섞이지 않게 별도 브랜치에 고정한다.
- RESULT: PASS
- DETAILS: detached HEAD 였던 `jev-personal` 워크트리에 브랜치 `feat/jev-personal` 을 만들고 4파일(+80)을 커밋 `8988aff`. 워크트리 작업 로그는 `.gitignore` 의 `.claude/*` 대상이라 커밋에서 제외(로컬 기록). **미푸시** — 푸시는 사용자 판단.

## 2026-09-20 00:05 — NEXT-SESSION 갱신
- WHY: Jev 완료·커밋 사실과 정정된 커밋 상태를 SoT 에 반영한다.
- RESULT: PASS
- DETAILS: 최상단 A절의 Jev 항목을 "완료·커밋됨"으로 교체하고 다음 한 걸음을 Archify 개념 설명으로 이동. 9/13 절의 `HEAD = c99a80d`·미커밋 47개 기술과 B절 「미결」의 Phase 1·2·3 미커밋 기술을 실측(`3229576` 푸시 완료 / Instapaper 9건 잔여)으로 정정했다.

## 2026-09-20 00:10 — DELEGATE-DONE vibe-ai-config Phase 5·6·4 위임 완료
- WHY: Codex pane `%11` 한도 소진 → 종료하고 Claude pane `%12` 로 교체해 배치 개선 잔여를 완주시켰다.
- RESULT: PASS
- DETAILS: 커밋 `0101c3a` (미푸시). 전제 정정 — Phase 4·5·6 본체는 이미 `3229576` 에 있었고 실제 잔여는 §6-Z·§7-B 였다. Phase 5 는 `analyze` PreToolUse matcher 가 `Read|Edit|Write` 라 `php-encoding-check.sh` 가 Codex 에서 영구 미발화이던 것을 `apply_patch` matcher + `file_path` 폴백 한 쌍으로 수정하고 `test.sh` matcher 린트로 승격. Phase 6 은 `_unused/` 회수원 3 + 빈 매니페스트 1 삭제 + 사문 참조 4종 제거. Phase 4 는 `~/.zshrc` 빈 줄 누적을 awk 버퍼링으로 교체. VERIFY: `test.sh` PASS=41 FAIL=0 · 린트 음성 실측 FAIL=1 · `apply_patch` EUC-KR→deny/UTF-8→통과 · zshrc 3회 멱등 diff 무출력 · `smoke-hooks.sh` 30건 실패 0 · `bash -n`·`shellcheck`·`jq` 무출력. Instapaper 9건 무변경 실측 확인.

## 2026-09-20 00:12 — CHECK 공용경로 심링크 위험 실측
- WHY: 위임 보고의 최우선 경고를 메인이 직접 재확인한다.
- RESULT: PASS
- DETAILS: `readlink -f ~/.local/bin/vibe` = `/Users/eunsol/Project/vibe-ai-config/.git/worktree/chore-archify/shared/scripts/tmux-suite/vibe.sh` — 보고대로 `chore-archify` 워크트리를 가리킨다. 워크트리 삭제 시 `vibe`·`pane-*`·`claude-delegate` 동시 dangling. 반면 `readlink -f ~/.agents/skills/archify` 는 자기 경로를 반환(실물 디렉터리)이라 archify 스킬은 이 위험의 대상이 아니다 — 브리핑의 "스킬 심링크가 worktree 를 가리킨다" 기술을 이 실측으로 좁혔다.

---

## 웹 리서치 자동화 엔진 (PRO-132) — 요구사항 재정의 + A-1 착수

## 2026-09-20 00:20 — CHECK 오늘 생성 프로젝트 식별
- WHY: 사용자가 "오늘 생성한 웹 처리 자동화 엔진 프로젝트"를 확인해 달라고 요청했다.
- RESULT: PASS
- DETAILS: 볼트 `01.Projects/`·`~/Project` 에는 없었고 **노션 Projects DB 의 「🔬 웹 리서치 자동화 엔진」(PRO-132, 2026-09-19 05:28 생성, status=Planning)** 이었다. Playwright/Puppeteer 없이 raw CDP 로 캡처·리플레이하고 가능한 것은 HTTP 로 승격하는 구조. 갭 2건 확인 — 볼트에 프로젝트 폴더 없음(집 정리 PRO-129·벌크업 PRO-128 과 같은 패턴) · 코드 레포 미생성.

## 2026-09-20 00:35 — DECIDE 유스케이스 → A/B 축 분리
- WHY: 사용자가 제시한 실제 유스케이스 3건이 문서의 "무인 반복 조사" 전제와 어긋났다.
- RESULT: PASS
- DETAILS: 3건 중 2건(찜·장바구니, 위시 등록)은 **스케줄 반복 대상이 아니라 사용자 트리거 1회성**이라 "반복 실행 시 AI 토큰 불필요" 라는 헤드라인 목표가 적용되지 않는다. A(온디맨드 액션 실행기) / B(무인 반복 조사기)로 분리하고 기존 Phase 1~5 를 B축으로 명시했다. 결정 4건 — ①유스케이스 3은 `notion-budget` 스킬의 「상품 페이지 → 위시 등록」 절이 이미 구현(asset-intake 판정: 개인유사) 이므로 재개발 금지 ②찜·장바구니는 멱등성이 없어 HTTP 승격 대상에서 제외 ③A축에는 raw CDP 직접 구현이 불필요(chrome-devtools-mcp/Aside 가 이미 그 역할) — raw CDP 는 B축 launchd 무인 실행에서만 값을 한다 ④쓰기 액션 성공 판정은 "눌렀다" 가 아니라 "상태 재조회에서 보인다".

## 2026-09-20 00:45 — CHECK 브라우저 기동 경로 실측
- WHY: A축 런타임을 확정하려면 어느 브라우저 경로가 실제로 붙는지 확인해야 한다.
- RESULT: PARTIAL
- DETAILS: 사용자 메인 Chrome(PID 45478)이 이미 `127.0.0.1:9222` 로 CDP 를 열고 있고 `DevToolsActivePort` 파일도 존재하는데 **`chrome-devtools-mcp`(`--autoConnect`) 는 그 파일을 못 찾는다며 연결 실패** — 원인 미규명으로 남겼다. 전용 프로필로 띄운 임시 Chrome(PID 30580)은 IPv4 9222 선점 때문에 `[::1]:9222` 로만 바인딩돼 정리했다(프로필 `~/.cache/web-research-engine/chrome-profile` 잔존). 사용자 지시로 **Aside 경로로 전환** — Aside 브라우저는 네이버 로그인 세션 보유가 확인됐다.

## 2026-09-20 01:05 — VERIFY-PASS A-1 스마트스토어 찜 recipe 실측
- WHY: A축 첫 recipe 로 스마트스토어 찜 동작을 끝까지 실행·검증·원복한다.
- RESULT: PASS
- DETAILS: 대상 `brand.naver.com/keychron/products/12700487031`. **찜 → 목록 검증 → 찜 해제 → 재검증 전 과정 성공** (186 → 187 → 186 복귀, 기존 186건 무변경). 셀렉터 설계 원칙 4건을 실측으로 확정 — ①CSS 해시 클래스 금지(`WgiX4L2Zw8` 단독이 버튼 10개에 매칭되는 범용 클래스) ②지연 렌더링 대기 필수(openTab 직후엔 `선물하기` 만 존재, role+name 조회 0건) ③추천 상품과 구분 필수(`찜` 포함 버튼이 페이지에 55개 — 대상 2개 + 추천 53개, 영역 스코프 없으면 엉뚱한 상품을 찜함) ④성공 판정은 `aria-pressed` false↔true(화면 텍스트는 `찜하기` 로 불변이라 텍스트 판정 불가). Aside 실측 제약 3건도 기록 — `aside repl` 은 호출마다 새 세션 · `allInnerTexts()` 미지원 · `snapshot(page,{selector})` 로 컨텍스트 절약 가능. 미확인: 찜 클릭 시 네트워크 요청(B축 승격 대상 아니므로 우선순위 낮음).

## 2026-09-20 01:30 — DESIGN action recipe 포맷 v0.1 + 오늘의집 대조 검증
- WHY: A-1 실측 결과를 재사용 가능한 포맷으로 고정하고, 두 번째 사이트로 일반성을 검증한다.
- RESULT: PASS
- DETAILS: 포맷 정본 초안을 스크래치패드 `action-recipe-v0.1.md` 에 작성(레포 생성 시 이관 대상). A-1 실패 양상 4개 → 필드 4개 대응(`target_scope` 필수 · `element` role+name · `wait` 지연렌더링 · `state.attribute` 성공판정/멱등성) + `login_gate`·`verify.external`·`revert`. 오늘의집(`store.ohou.se/goods/107013`) 읽기 전용 조사로 포맷 검증 — **모든 필드가 값 또는 명시적 `확인 못 함` 으로 채워져 통과**. 대조 결과: 오늘의집은 **미로그인** 상태라 찜 목록 URL 확인 못 함 · 대상 버튼 이름 `스크랩하기`(추천은 `스크랩` 이라 구분 유리) · 화면 텍스트가 라벨이 아니라 **스크랩 개수(181,186)** · 대상 버튼에 `aria-pressed` 없음(추천 버튼엔 있음 — 미로그인 관찰이라 로그인 후 재확인 필요) · 지연 렌더링 없음 · 버튼 총 27개(대상2+브랜드1+추천24). v0.1 의 "상태 속성 없으면 멱등성 불가 명시" 규칙이 두 번째 사이트에서 즉시 발동했다.

## 2026-09-20 01:32 — NOTE 정리 미완 1건
- WHY: 삭제는 사용자 승인이 필요하다.
- RESULT: BLOCKED
- DETAILS: `~/.cache/web-research-engine` (149M, 내가 만든 Chrome 디버그 프로필 — Aside 경로 채택으로 불필요해짐) 삭제가 권한 거부됐다. 사용자가 `rm -rf ~/.cache/web-research-engine` 직접 실행 필요. 사용 중 프로세스 없음은 확인했다.

## 2026-09-20 01:55 — VERIFY-PASS 오늘의집 스크랩 recipe 완주 + 포맷 v0.2
- WHY: 로그인 후 오늘의집 recipe 를 완성하고 recipe 포맷의 일반성을 두 번째 사이트 쓰기로 검증한다.
- RESULT: PASS
- DETAILS: `store.ohou.se/goods/107013` 스크랩 → 목록 검증 → 해제 → 재검증 완주(상품 102→101, 전체 124→123 복귀, 대상 외 무변경). **포맷 결함 3건 발견 — 두 번째 사이트를 실제로 써보지 않았으면 못 찾았을 것들이다.** ①(심각) 오늘의집은 로그인 후에도 `aria-pressed` 가 없고 **접근성 이름 자체가 토글**된다(`스크랩하기`↔`스크랩 취소하기`) → `element.name` 고정 시 **이미 스크랩된 상품에서 버튼을 못 찾아 실패**. 탐색용 `name_any` 와 상태 판정용 `state.on/off` 분리로 수정 ②`state` 표현 방식이 `attribute`·`accessible_name`·`none` 최소 3종 → `state.by` 로 선언 후 분기 ③오늘의집 스크랩북 URL 에 **사용자 ID 가 박혀 있어**(`ohou.se/users/5561554/collections`) 고정 불가 → `verify.external.nav`(네비게이션 경로)를 1급으로. 부수: emotion 해시 클래스는 스크랩 전후 불변이라 탐색·판정 양쪽에 무용(원칙 1 재확인) · 목록 상품명에 판매자명이 접두되므로 `match_by` 는 부분 일치여야 함 · 해제 경로는 목록 UI 가 아니라 상품 페이지 동일 버튼. login_gate 지표 3종 확정.

## 2026-09-20 02:20 — CHECK 쿠팡 찜 조사 (읽기 전용) + 포맷 v0.3
- WHY: 세 번째 사이트로 recipe 포맷의 일반성을 검증하고, 403 이력이 있는 쿠팡을 A축에 넣을지 판단한다.
- RESULT: PASS
- DETAILS: **403·캡차·봇 차단 없음**(로그인 상태) — 볼트의 2026-08-22 403 기록이 미로그인 조건이었음이 재확인됐다. 쿠팡은 쉬운 축과 위험한 축이 갈린다. 쉬운 쪽: 페이지 내 찜 버튼 **1개뿐**(추천 영역에 없음)이라 `target_scope` 부담 없음 · 목록 URL 이 고정 경로에 개인 식별자 없음(`wish-web.coupang.com/wishInitView.pang`) · 지연 렌더링 없음. **깨진 가정 2건**: ①찜 버튼에 **접근성 이름이 아예 없다**(`aria-label`·이름·텍스트 전무) → `role`+`name` 타기팅 원리적 불가, 유일 수단이 SVG path(`path[d^="M12.174"]`)인데 해시 클래스보다 나을 게 없다 → `element.tiers` 탐색 계층 + `fragility: HIGH` 도입, HIGH 면 실행 전 전제조건 검사 의무화 ②(심각) **상태를 페이지에서 못 읽는다** — v0.2 가 예고만 했던 `state: none` 첫 실물. 찜은 토글이라 이미 찜된 상품을 다시 누르면 **해제된다** → "찜해줘"가 "찜 해제"로 조용히 뒤집힘. 네 번째 방식 `state.by: external_list`(목록 선조회, cost HIGH) 추가 + **사전 상태 확인 없는 토글 실행 금지** 규칙을 포맷에 명문화. 3사이트가 모든 축에서 달라 포맷 검증에 최소 3개가 필요했음이 확인됐다. 미확인: 클릭 후 변화(쓰기 테스트 필요) · 목록에서의 해제 UI. 부수: 쿠팡 로그인 표시가 `김*우 님` 으로 타 사이트(`소아은솔`)와 달라 자동화 대상 계정 확인 필요.

## 2026-09-20 02:30 — CORRECT 쿠팡 계정 의문 해소
- WHY: 직전 항목에서 "쿠팡 로그인 표시가 타 사이트와 달라 계정 확인 필요"로 남긴 건이 사용자 확인으로 해소됐다.
- RESULT: PASS
- DETAILS: **세 사이트 모두 같은 계정이다.** 쿠팡은 **실명**(`김*우 님`), 오늘의집은 **닉네임**(`소아은솔`)을 표시할 뿐이다. 설계에 주는 함의: `login_gate` 판정에 **사용자명 문자열을 사이트 간에 공유하거나 기대값으로 하드코딩하지 않는다** — 표시 체계가 사이트마다 다르고(실명·닉네임·마스킹) 닉네임은 사용자가 바꿀 수도 있다. 지표는 **개인 영역의 존재 여부**(장바구니·찜 목록·프로필 메뉴 링크)로 잡고 이름 문자열은 보조 근거로만 쓴다. 노션 PRO-132 의 해당 서술도 정정했다.

## 2026-09-20 02:50 — CREATE 레포 `web-research-engine` 초기화 + 실행기 스켈레톤
- WHY: recipe 정본 3종과 포맷 스펙이 스크래치패드(휘발성)에만 있어 유실 위험이 실질적이었다.
- RESULT: PASS
- DETAILS: `~/Project/web-research-engine` 생성(이름·위치는 노션 프로젝트명에 맞춘 기본값 채택), 커밋 `bad6b31` 8파일 802줄. **리모트 미설정·미푸시.** 구성 — `docs/recipe-format.md`(포맷 정본 v0.3) · `docs/findings-2026-09-20.md`(3사이트 실측 근거, 스크래치패드에서 이관) · `recipes/{naver-smartstore,ohou,coupang}.yaml` · `src/run-recipe.mjs`(실행기 스켈레톤) · README(A/B 축 + 안전 규칙 5종). 실행기는 recipe 로드·**안전 게이트**·실행 계획 출력까지 구현했고 `node --check` 통과. 게이트는 차단만 하지 않고 사유·영향·대체 경로 3줄을 함께 낸다(가드 설계 원칙). 미구현 TODO 2건 — ①YAML→JSON 빌드 단계(런타임 의존성 0 원칙 때문에 파서 미도입) ②`buildReplScript()`: `aside repl` 이 호출마다 새 세션이라 한 액션 전 과정을 단일 REPL 호출용 JS 로 생성해야 한다.

## 2026-09-20 02:55 — HANDOFF Claude Code → Codex
- WHY: 사용자가 Codex 로 작업을 이어가기로 했다.
- RESULT: SAVED
- DETAILS: `NEXT-SESSION.md` 최상단에 신규 착수 지점 절을 만들고 이전 Archify·Jev 절을 아래로 내렸다. 인계 내용 — 핵심 판단 4건(A/B 축 분리 · 유스케이스 3은 `notion-budget` 기존 자산 · A축에 raw CDP 불필요 · `chrome-devtools-mcp` 연결 실패 원인 미규명) · 완료분 · 다음 한 걸음 3개 · 타협 금지 안전 규칙 · Single-Writer 및 레포 3곳 상태 · 열린 pane. **미결**: 쿠팡 쓰기 테스트가 인계 시점에 Aside 에서 실행 중이었다(상품 페이지 탐색 단계, **찜 클릭 전**). 완료 시 스스로 해제하도록 지시돼 있으나, 중간에 죽었으면 찜 목록에서 `국내산 애플민트, 10g, 1개` 잔존 여부를 확인하고 해제해야 한다 — 이 지침을 인계 문서에 명시했다.
