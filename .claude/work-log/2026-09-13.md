# 2026-09-13 작업 로그

## Codex 메인 전환 가능 여부 점검 → P4 리허설 완주 (R12 제외)

**질문**: "현재 코덱스를 메인으로 설정해도 되는지 확인해". 스크래치 점검 스크립트(`codex-readiness.sh`)로 읽기 전용 실측.

### 착수 시 블로커 3건 → 전부 해소
1. **배포 드리프트** — 9/9 이후 정본 12커밋 미배포(AGENTS.md 불일치·스킬 1개). 사용자가 `install.sh personal` 실행 → 드리프트 0(head `0720742`).
2. **훅 미신뢰** — `[hooks.state]` 없음 = 6종 fail-open. `/hooks` 진입 시 **`0.153.4` 가 `hooks.json` 최상위 `_note` 키를 거부**(`unknown field _note, expected description`) → Codex pane(%4)에 위임해 `_note→description` 리네임(`c9f9227`). 재진입 후 사용자 `t` → `trusted_hash` 6건 기록.
3. **notion OAuth** — 사용자 `codex mcp login notion` 완료.

### 발견·수정 (모두 `vibe-ai-config`, Codex %4 pane 위임)
- **Codex 가 관리 구획 안에 테이블을 쓴다** — `[tui.model_availability_nux]`·`[hooks.state…]` 6건이 `# END VIBE MANAGED TUI` **앞**에 삽입됨(Codex 는 문서 끝·후행 주석 앞에 append). 규율 ②(외부 줄 → 중단)로 소실은 막았지만 다음 install 이 막히는 상태. **9/8 hooks.state 소실 사고의 원인이 이 구조**(당시엔 구획 전체 교체). 수정 `226d109`: 외부 실질 줄을 END 뒤로 보존 이동. 재배포 후 해시 6건 유지, doctor 0 warn. Codex 판단: 장기적으로 TUI 구획을 파일 끝이 아니라 ROOT 직후에 두는 게 맞음(미착수).
- **`vibe` 가 Codex 셸에서 `command not found`** — zsh alias 뿐, 공용경로 `~/.config/vibe-tools/vibe.sh` 는 PATH 밖. `codex/AGENTS.tail.md` 가 `vibe delegate` 를 기본 위임 경로로 지시하는데 실제로는 죽어 있었음. 수정 `f311099`: tmux-suite `install.sh` 5단계 `~/.local/bin/vibe → vibe.sh` + `vibe.sh` 심링크 실경로 해석(lib/ 탐색 결함 노출됨). 대화형 alias / 비대화형 PATH / 직호출 = 같은 파일 1개.
- `TMUX` 환경은 Codex Bash 에 전달됨(R3 ✅) — `shell_environment_policy` 문제 없음.

### P4 리허설 (계획서 §4-1 R1~R13)
- 새 Codex 세션을 `vibe delegate para --tool codex` 로 띄워 자가 점검. **1차(%19)는 Codex 업데이트 프롬프트(0.153.4→0.154.0)에 주입 메시지가 "1. Update now" 를 눌러 업그레이드 후 종료** → pane 폐기, %20 재기동.
- 결과: R1~R11·R13 **전부 ✅** (0.154.0 기준 — 훅 6종 동작·rules 3결정·notion 조회·vibe 실행). R12(`VIBE_HUB_TOOL=codex vibe main` 육안)만 잔여.
- 교훈: **위임 pane 기동 직후 업데이트 프롬프트가 뜨면 주입 메시지의 첫 글자가 메뉴 선택으로 소비된다.** `vibe delegate` 가 Codex 기동 시 업데이트 프롬프트를 억제하거나(설정/플래그 확인 필요) settle 후 화면을 확인하는 보강 여지.

### 사용량 실측 (전환 판단 참고)
- Codex 상태줄: `5h 80% left · weekly 97% left`(12:09). 9/9 Claude 메인 유지 근거였던 "사용량"은 현재 Codex 쪽 여유가 큼.

### 커밋·푸시 (vibe-ai-config, 전부 origin 반영)
`35c0068` status · `c9f9227` hooks.json · `226d109` install relocate · `e418e00` status · `f311099` vibe PATH

### 잔여
- 토글 확정 여부 = 사용자 결정. 확정 시: `~/.zshrc.local`·`vibe-dotfiles/zsh/zshrc.local.template` `VIBE_HUB_TOOL=codex` · para `AGENTS.md` 분업 절 · 4-2 스냅샷 Codex 섹션 · R12.
- P3-1 `vibe-dotfiles/AGENTS.md` 미착수(fallback 동작 중, 비블로커).
- `stash@{0}` 드롭 · Claude `settings.json` statusLine 파이프 정리 · 8/29 이월 2건.
- 리허설 pane %20 정리(`vibe reap`).

## 토글 확정 → Codex 메인 전환 착수 (12:1x, 사용자 결정)

- 완료: `~/.zshrc.local` `VIBE_HUB_TOOL="codex"` · `tmux set-environment -g VIBE_HUB_TOOL codex` · para `AGENTS.md` 「도구 분업 & 위임」 절 재작성(분업 발효·전환 이력·Codex 운영 함정 3건·위임 기본값 codex).
- 위임 중(Claude 종료 시점에 미완, Codex pane 이 독립 진행): %4 vibe-ai-config [A] 스냅샷 Codex 섹션 [B] 업데이트 프롬프트 억제 → install → 커밋 2건 / %21 vibe-dotfiles template `codex` 커밋 1건. 둘 다 push 안 함 — 다음 세션 push.
- 사용자 요청으로 상태 저장 후 Claude Code 종료. 다음 세션은 `vibe main` = Codex 허브. NEXT-SESSION 상단 인계 블록 참조.

## 작업 목적별 Codex 모델 프로필 설정

- 기본값은 기존 `gpt-5.6-terra` / `medium`을 유지했다. 일반적인 구현·작업 관리를 균형 있게 처리한다.
- `~/.codex/routine.config.toml`: `gpt-5.6-luna` / `low` — 빠른 조회·요약·분류·단순 문서 수정.
- `~/.codex/review.config.toml`: `gpt-5.6-sol` / `medium` — 코드 리뷰·다단계 분석·일반 구현 검증.
- `~/.codex/deep.config.toml`: `gpt-6-astra` / `high` — 설계 판단·복잡한 디버깅·보안 검토.
- 실행: 각각 `codex -p routine`, `codex -p review`, `codex -p deep`. 세 프로필 TOML 파싱과 `codex --strict-config --profile routine exec --help` 검증 통과.
- 동기화 정본 반영은 `vibe-ai-config` 메인 워킹트리에 기존 untracked `.codex/`가 발견되어 Single-Writer 규율상 중단했다. 정본·설치 스크립트는 수정하지 않았다.

## Codex 위임 호환·모델 프로필 동기화 및 검증

> **후속 감사 정정:** 아래는 당시 실행 기록이다. 프로필 정상 판정은 철회한다. 현재 CLI에서 `[profiles.*]` 충돌로 세 프로필이 실패함을 재현했으며, 상세와 Claude 인계는 이 파일 마지막 절 및 [[01.Projects/개인컴_AI_작업환경_업그레이드/codex-이식기능-감사-20260913]]을 따른다.

- `vibe delegate` Codex 호환 수정: `vibe-ai-config` `13d561f` — `TMUX` 조회가 안 되는 Codex 흐름에서도 `TMUX_PANE`으로 현재 pane을 판정한다. pane을 특정할 수 없으면 임의 세션을 선택하지 않고 para 허브를 여는 완전한 재진입 명령을 안내한다. 실제 `~/.local/bin/vibe`는 정본 `vibe.sh`를 직접 가리키며, `test-vibe-delegate.sh` 회귀 테스트가 통과했다.
- 모델 프로필 정본·설치기: `c99a80d` — `codex/model-profiles.toml`의 routine(luna/low)·review(sol/medium)·deep(astra/high)를 `install.sh personal`이 멱등 배포한다. 임시 HOME에서 사용자 기본 모델·프로젝트 trust·프로필 사용자 키 보존 및 2회 설치 바이트 안정성을 확인했다.
- 라이브 배포 완료: 종료 마커가 사라진 `~/.codex/config.toml`의 관리 구획은 `[tui].status_line` 직후에 `# END VIBE MANAGED TUI` 한 줄만 복구해, 후속 Codex 외부 상태 32개를 보존했다. `install.sh personal` 성공 후 세 프로필·기본 `gpt-5.6-terra`·hooks trust state 보존을 확인했고 `codex --strict-config doctor`에서 설정 파싱이 통과했다.
- 동기화 점검: `drift-report.sh --run`은 정본 일치를 보고했다. `smoke-hooks.sh --run --with-contract`의 계약 회귀는 16/16 통과했지만, macOS에 `timeout`이 없어 스모크 실행 15건은 rc=127로 미실행이다(별도 이슈).
- 남은 환경 이슈: `codex doctor`는 `memories_1.sqlite` 열기 실패 및 이 실행 환경의 DNS/네트워크 차단으로 WebSocket·Notion reachability 경고를 낸다. 설정 파싱·MCP 등록과는 별개이며 아직 복구하지 않았다.

## 집 정리 사진 인벤토리 준비

- Notion `🏠 집 정리`(PRO-129)에 사진 기반 물품 인벤토리, 구역별 촬영 체크리스트, 인식 결과 기록표를 추가하고 재조회로 저장을 확인했다.
- 다음 행동: 입구 전경·책상/서랍·침대/침대 아래·옷장/행거·바닥/임시 적치물 사진을 올리면 물품·수량·처리 후보·확인 사항을 기록한다. 사진만으로 처분·구매를 확정하지 않는다.

## Codex 이식 기능 감사 완료 → Claude Code 인계

- 사용자 요청: 분석 내용을 저장하고 Codex 대화를 종료, Claude Code에서 설정 수정 재개.
- Luna/medium 내장 조사 에이전트 3개로 읽기 전용 감사. 상세: [[01.Projects/개인컴_AI_작업환경_업그레이드/codex-이식기능-감사-20260913]].
- 재현된 최우선 오류: `codex --profile routine mcp list` exit 1, review/deep도 동일. 설치기의 legacy `[profiles.*]` 배포를 별도 프로필 파일 형식으로 고쳐야 한다. 이전 정상 배포 보고는 검증 부족으로 철회.
- 설정 목록: 글로벌 스킬 59개, 활성 플러그인 22개(사내 9개), 공통 훅 6개, 이식 에이전트 정의 4개. 전체 실동작 보장은 아님.
- 정상 확인: Notion 실제 조회, rules 대표 allow/prompt/forbidden, 위임 스크립트 심링크. 미검증: 이식 에이전트 이름별 호출, 전체 훅 Active, 스킬별 MCP 호환.
- 다음 순서: 프로필 설치 형식 → 위임 기본값 → 에이전트 호출 → Codex 전용 훅 계약. DB 접근 실패와 실제 손상을 구분하고, 훅 신뢰를 임의 변경하지 않는다.
- NEXT-SESSION 최상단을 최신 인계로 수정. 커밋·푸시·세션 강제 종료·설정 수정은 수행하지 않는다. 사용자가 Claude Code를 실행해 이어간다.

## Codex 프로필 오류 수정 완료 + shared/claude/codex 배치 감사 → 개선 계획 (Claude Code, 17:0x~17:3x)

- 프로필 수정(`vibe-ai-config`, 미커밋): `codex/model-profiles.toml` 삭제 → `codex/model-profiles/{routine,review,deep}.config.toml` 분리. `install.sh` `_codex_profiles_apply`(레거시 `[profiles.*]` 생성) → `_codex_profiles_purge_legacy`(구획 제거·외래 키/구획 밖 테이블 발견 시 삭제 대신 중단+대체 명령) · `install_codex_profiles()` 신설(tomllib 검증·내용 다를 때만 백업) · `_codex_config_verify` 단언을 profiles/루트 profile **부재**로 반전 · `codex/README.md` 정정.
- VERIFY: `bash -n` 0 · `shellcheck` 전후 동일(SC2155 1건 기존) · `install.sh personal` ❌ 0 · tomllib 독립 대조 hooks.state·model·mcp_servers·projects·tui 동일 · **`codex --profile routine|review|deep mcp list` 3개 exit 0**(메인이 routine·deep 재실행 확인) · 2회차 `diff` 무변경. 부작용: `status/personal.md` 는 설치기 산출물이라 12:28판→17:12판 재생성됨(손편집 없음, 되돌리지 말 것).
- 배치 감사(읽기 전용, Explore 에이전트 2개 병렬 — 훅 축·비훅 축): `shared/` 구조(core+shims·rules·commands·sops)는 규율 성립. 위반은 전부 "공용인데 `claude/` 밑" — `claude/mcp.base.json`(Codex `install_codex_mcp` 가 직접 파싱) · `claude/hooks/delegate-pane-guard.sh`(도구 무관, Codex 위임 시 미주입 구멍) · `claude/plugins/` 10개 중 9개를 Codex 가 `enabled=true` 로 로드 · `shared/skills/` 빈 디렉터리인데 루트 CLAUDE.md 가 SSoT 라고 기술(실제 정본은 루트 `skills/`). 공용인데 Codex 미배선: `shared/sops`(`~/.codex/sops` 부재) · smoke/drift/test.sh 가 Claude 설정만 검사. 사문: `activity-logger.sh`·`claude-md-capture-reminder.sh`(8/31 해제). 중복 아님으로 정정: `delegation-preamble`(.sh 는 로더, .md 가 SSoT) · `config-drift-check.sh` 양쪽(대상 다름, 단 Codex 판이 인라인 재구현 + `claude/*.json` 직접 참조).
- 부수 발견: `readonly-bash-allow.sh` ↔ `bash-chain-guard` 충돌(deny 가 allow 를 이김 — 이 세션 `codex … | head` 차단 실측) · `shared/hooks/README.md` 배선 상태 절 낡음.
- 산출: [[01.Projects/개인컴_AI_작업환경_업그레이드/shared-배치-개선계획-20260913]] — Phase 1(P0 4건+preamble 로더 디커플링, 한 세션) → Phase 2(Codex 배선 확장) → Phase 3(chain-guard 결정) → Phase 4(plugins 재배치, 별도 설계·푸시·양쪽 재설치 동반). 결정 필요 5건 §7. NEXT-SESSION 1번 항목 ✅ 표기 + 계획 링크.
- 하지 않은 것: 배치 이동 0건 · 커밋·푸시 0건 · 브리핑 2번(위임 기본값)·3번(에이전트·훅 실동작) 미착수.

## 플러그인 감사(Opus 2축) → 개선 계획 통합 재작성 (Claude Code Fable, 17:4x~18:0x)

- 사용자 요청: 플러그인도 조사해 계획에 포함, 조사는 다른 모델로 → Explore×2 를 **Opus** 로 실행(A: tmux-suite·git-suite·dev-suite·analyze·notify / B: work-suite·task-mgmt·personal-suite·google-workspace·vibe-admin-mcp·_unused·marketplace). 읽기 전용, 수정 0.
- 실동작 결함(계획 Phase 3 편입): `git-guard.sh` 가 `{"continue":false}` 출력(Codex PreToolUse 즉사 형식, hooks.state 등록됨) · `analyze` PostToolUse 19개 matcher 가 표현식 문법 → 미발화 가능 + `console.log(d)` 입력 재출력 · `auto-version-bump` 미작동 실측(tmux-suite 1.0.73 번호 동일·`vibe.sh` 내용 3곳 상이) · `pane-state.sh` `grep [c]laude` → Codex pane 영원히 유휴 · task-mgmt matcher `Edit`≠`apply_patch`·`systemMessage` 출력 Codex 스키마 밖.
- 구조 발견(Phase 4 재설계 근거): tmux-suite 실사용은 `~/.config/vibe-tools` 심링크→레포 직결, 플러그인 캐시는 사문(preamble 로더 5단 `..` 가 캐시에서 fail-open) · `references/*.md` 소비 스킬 8건이 레포 절대경로 참조 → 디렉터리 이동 시 파손 · `google-workspace` 껍데기(`gws-quickref` 참조 0·`cal-to-notion.md` frontmatter 없음) · Codex 는 커맨드를 `migrated-command-skills` 로 변환하는데 8개 중 3개만(원인 미규명).
- 보안: `vibe-admin-mcp` 키 파일 `config.json` 이 어느 `.gitignore` 에도 없음(현재 미존재) · 키 설정 절차는 `_unused/vibe-admin/outline-setup.md` 에 고립 → P0 1-E.
- `_unused` 판정: 회수 후보 `write-guard.sh`(시크릿 스캔 — 현행 전체 부재)·`bash-guard.sh`(파괴 명령 6규칙)·`outline-setup.md`·`review-mr.md`(GitLab) / 폐기 9건(조상·깨짐). 유령 설치 `harness`·`test`·`vibe-admin` 이 `installed_plugins.json` 잔존.
- 산출: [[01.Projects/개인컴_AI_작업환경_업그레이드/shared-배치-개선계획-20260913]] 전면 재작성 — Phase 1(P0 6건, 1-E gitignore 즉시) · 2(Codex 배선) · 3(운영 결함 6건 표) · 4(플러그인 A' 권고 = 스크립트만 `shared/scripts/`, 포장은 잔류·문서화) · 5(Codex 호환: 훅 출력·매처 규약 명문화, 커맨드 매핑 표, 에이전트 4종 Claude 전용 확정) · 6(사문·유령·_unused 회수/폐기) · 결정 필요 8건.
- 하지 않은 것: 수정·이동·커밋 0건. 브리핑 2번(위임 기본값) 미착수. 3번(에이전트 실동작)은 계획 5-C 로 "이식 대상 아님" 결론 제안.

## 배치 개선 Phase 1 실행 완료 (Claude Code Opus, 18:0x~18:2x) — vibe-ai-config 미커밋

계획 [[01.Projects/개인컴_AI_작업환경_업그레이드/shared-배치-개선계획-20260913]] §1 전 단계 게이트 통과. 브랜치 `master`(계획서 `main` 표기 정정). 커밋·푸시 0.

- **1-E** 🔐 `.gitignore` 에 `claude/plugins/vibe-admin-mcp/config.json` — Outline API 키 파일이 어느 gitignore 에도 없던 것(`client.ts` 가 `${CLAUDE_PLUGIN_ROOT}/config.json` 에서 읽음). `git check-ignore -v` 히트 확인.
- **1-A** `claude/mcp.base.json` → `shared/mcp.base.json` + 참조 6곳(`install.sh` BASE_MCP·주석 3, `codex/hooks/config-drift-check.sh` MCP_CANON, `docs/settings-inventory.md` 2행). `$CONFIG_SRC` 타 용처 무변경. `codex mcp list` 정본 3종 일치.
- **1-B** `delegate-pane-guard.sh` → `shared/hooks/core/` + 출력을 `{verdict,reason,context}` 코어 계약으로 재작성 + `manifest.json` 등록(claude·agy·codex/session_start) + `settings.base.json` 을 `shims/claude.sh` 경유로 재배선 + **`codex/hooks.json` SessionStart 항목 신설**(누락돼 있던 Codex 구멍 폐쇄, `_note` 키 없이 기존 형식 복제). 메인 실측: SessionStart 입력+`VIBE_DELEGATED=1` → 억제문구, env 없으면 무출력.
- **1-C** 사문 훅 2종(`activity-logger.sh`·`claude-md-capture-reminder.sh`) → `claude/hooks/_unused/` (삭제 아님 — settings-inventory 보존 사유). `claude-md-setup/SKILL.md` 의 살아있다는 서술 정정.
- **1-D** `shared/skills/` 빈 디렉터리 제거 + 루트 `CLAUDE.md` 트리에서 허위 SSoT 표기 삭제, 루트 `skills/` 에 "3도구 정본·npx 규약상 루트 스캔·shared 동급" 명문화.
- **1-F** 사문 참조 3건: `claude-callback.sh` 제거(+`docs/architecture-audit.md` 예시 교체) · `marketplace.json` git-suite 설명을 실제 이벤트(PreToolUse/PostToolUse)로 정정(version 무변경) · `shared/hooks/README.md` 배선 상태를 6훅×3도구 표로 재작성(chain-guard 기본 deny 명시).
- **4-1a** `delegation-preamble.sh` 정본 탐색을 `VIBE_AI_CONFIG_ROOT` → `git rev-parse --show-toplevel` → 구 5단 `..` 폴백 순으로. fail-open → stderr 경고. 고아 사본 실측 rc=1/경고, env 설정 시 rc=0.
- **4-1b** `references/{plane,notion}-common.md` → `shared/references/` + 소비 스킬 **7개 SKILL.md 10곳** 경로 갱신 후 재배포. 레포·`~/.agents/skills` 양쪽 구 경로 0건(메인 재확인).
- VERIFY: `bash -n` 0 · `shellcheck` 신규 경고 0(기존 SC2155 1건) · `jq` 5건 · `install.sh personal` ❌ 0 · 2회차 `~/.codex/config.toml`·`~/.claude/settings.json` diff 무변경(멱등) · `test.sh` PASS=16 FAIL=0 · `drift-report.sh` 드리프트 없음·shim 배선 5→6 · `codex --profile routine mcp list` exit 0(직전 프로필 작업 무손상). `smoke-hooks.sh` 15건 rc=127 은 `timeout` 부재 기존 이슈로 베이스라인 동일.
- 🔴 사용자 단계: Codex TUI `/hooks` 에서 신규 `delegate-pane-guard` 항목 `t` 재신뢰. `[hooks.state]` 임의 생성 안 했음.
- 신규 발견 → Phase 2: `shared/sops/sop_work_log.md` L7·95 가 해제된 `activity-logger.sh` 자동 append 를 약속(폐경로 `vibe-claude-plugin/claude-config/hooks/` 포함).

## ⚠️ 사고 — Stop 훅 자동 커밋·push 발화 (18:3x, 수습 완료)

- **경위**: 1-B 실동작 확인을 위해 `vibe delegate /Users/eunsol/Project/vibe-ai-config --tool codex` 로 pane %5·%6 생성 → 각 Codex 세션이 끝날 때마다 그 레포의 **프로젝트 Stop 훅**(`.codex/hooks.json` + `.claude/settings.json` → `auto-version-bump.sh` → `git-auto-commit.sh`)이 발화. 자동 커밋 3건 생성 + push 시도(실패).
- **원인**: 브리핑에 이미 적혀 있던 **"8/29 이월 위험 2건 — Stop 훅 auto-push"** 가 그대로 터진 것. 위험을 인지한 상태에서 해당 레포 cwd 로 에이전트 pane 을 띄운 판단 오류.
- **피해**: `git mv` 가 rename 을 stage 해둔 탓에 훅이 **그것만** 커밋 → `shared/mcp.base.json` 은 HEAD 에 있는데 참조를 고친 `install.sh` 는 미커밋 = **HEAD 가 깨진 상태**(`git grep mcp.base.json HEAD -- install.sh` 가 `$CONFIG_SRC/mcp.base.json` 반환). 파일 내용 손실은 0. push 실패로 외부 반영 없음.
- **부수 피해**: tmux-suite plugin.json·marketplace.json 버전이 `1.0.73 → 1.0.81` 로 **8회 bump**(반복 발화).
- **수습**(사용자 결정 2건): ① 양쪽 Stop 훅 블록 제거 + `.phase1-bak` 백업(훅 먼저 — reset 후 재발화 방지) ② `git reset --soft c99a80d` → HEAD 복구·Phase 1 작업 전부 보존 ③ 버전 bump 만 수동 원복(1-F 의 git-suite 설명 수정은 유지, `marketplace.json` 이 HEAD 대비 1줄 차이).
- 🔴 **미복구 상태**: `vibe-ai-config/.claude/settings.json`·`.codex/hooks.json` 의 Stop 훅이 **꺼져 있다.** Phase 2~6 종료 후 `.phase1-bak` 에서 복원할 것.
- **감사 판정 반증**: 계획 §3-D "`auto-version-bump` 미작동" 은 **틀렸다 — 작동한다.** Claude Stop 이 아니라 **Codex Stop** 에서 발화했고, 캐시 내용 불일치의 원인은 별개다. 계획 3-D 재작성 필요.
- **교훈**: 이 레포는 에이전트 세션 종료 자체가 쓰기 행위다. 확인은 `git -C` 로 para cwd 에서 하고, pane 을 띄워야 하면 Stop 훅을 먼저 내린다.

## 배치 개선 Phase 3 완료 (Claude Code Opus, 19:0x~19:3x) — vibe-ai-config 미커밋

3-A·3-B·3-D·3-E·3-F 수정, 3-C 실측만. 게이트 중단·롤백 0. HEAD `c99a80d` 유지. 변경 파일 8개(+`.codex/` 사본 1).

- **3-B** `git-guard.sh` 차단 4곳을 `{"continue":false,"stopReason"}` → `hookSpecificOutput.permissionDecision:"deny"` (Codex PreToolUse 즉사 형식 제거). 가드 설계 원칙대로 각 메시지에 대체 명령 완전형 추가(`reset --hard` → `git stash push -u` / `reset --soft HEAD~1` / `restore <파일>`). deny 8케이스·allow 4케이스 실측.
- **3-F** `task-mgmt/hooks.json` matcher `Edit` → `Edit|Write|apply_patch` · `task-status-done-reminder.sh`·`auto-retrospect.sh` 출력 `systemMessage` → `hookSpecificOutput.additionalContext`(후자는 Stop 훅이라 hookEventName=Stop). 잔여: `apply_patch` 의 `tool_input` 필드명 미문서화로 `file_path` 추출은 미수정(추측 금지).
- **3-E** `pane-state.sh` 에 `pane_tool_name()`·`pane_tool_args()`·`pane_tool_busy()` 신설, `VIBE_PANE_TOOLS` 로 하드코딩 제거, 기존 `pane_claude_*` 는 하위 호환 별칭 존치. `vibe.sh` 중복 pane 가드를 도구 무관으로(재기동은 claude 전용이라 타 도구는 신규 pane 폴백). `--ro`·`resume` Claude 전용 명시. **판정 반전**: `pane_current_command` 가 claude pane 을 `2.1.265`(버전 문자열)로 잡아 가드가 **Claude 에서도 이미 무효**였음 — Codex 한정 결함이 아니었다. 실측 %1=claude·%5/%6=codex·%2/%3=none 정확 판정.
- **3-D** `auto-version-bump.sh`(+`.codex/` 동일 사본) — ① pathspec 밖 staged 변경 감지 시 **커밋 건너뛰고 stderr 경고+직접 커밋 명령 안내** ② `git commit --only -- <자기 pathspec>` ③ 가드 시 push 도달 불가. `/tmp` 스크래치 레포에서 **사고 시나리오 재현 실패 = 수정 성공**(남의 rename 이 HEAD 에 안 들어감). 공용 라이브러리 `git-auto-commit.sh` 는 소비자가 이 훅 둘뿐이고 `-A` 계약이 정상이라 **미수정**, 훅에서 고아 `source` 제거.
- **3-A** `readonly-bash-allow.sh` 에 파이프·체이닝·멀티라인 early-exit(판정 포기) 추가 — chain-guard 와 판정 범위 분리. 변경 전후 대조 실측: **달라진 건 파이프·체이닝 5건뿐**(전부 이미 deny 되던 죽은 allow), 파이프 없는 읽기 allow 전건 유지. `bash-chain-guard.sh` 무수정.
- **🔴 3-C 실측 — `analyze` PostToolUse 19개 미발화 확정.** 가짜 `gradlew` 마커 프로브 + 양성 대조군으로 입증(직접 실행 시 마커 생성, Edit 경유 시 미생성). `console.log(d)` 19/19 전부 입력 JSON 재출력. → 고치면 19개가 갑자기 발화하고 index 0 이 Kotlin 편집마다 `./gradlew ktlintFormat` 을 실제 실행 → 결정 필요.
- VERIFY: `bash -n` 8건 · `shellcheck` 베이스라인 SC2009 3→1건(신규 0) · `jq -e` 3건 · `test.sh` PASS=16 FAIL=0 · `test-vibe-delegate.sh` PASS · `install.sh personal` ❌ 0 · 2회차 diff 무변경 · `drift-report` 드리프트 없음 · Stop 훅 `grep -c` 양쪽 **0 유지**.
- 부수 효과: 3-F 검증이 `~/.cache/personal-suite/last-sunday-nudge` 를 소모 → 오늘(일) 세션 종료 시 주간 루틴 안내 미출력 가능.
- 🔴 사용자 단계: Codex `/hooks` 재신뢰 — `hooks.json` 이 바뀐 건 **task-mgmt 1건**. `git-suite`·`personal-suite` 는 스크립트 본문만 변경이라 `Active` 열 확인 필요(추정 승인 금지).
- §7-A 복원 조건 충족(3-D 처리 완료) — 단 Phase 2·5·6 종료 후 복원.

## Phase 2 + 3-C 완료 · 노션 프로젝트 확인 · 카페 작업 종료 (19:4x~20:3x)

**Phase 2 + 3-C** (vibe-ai-config 미커밋):
- **3-C** analyze PostToolUse 19개 → 제외 5개(ktlintFormat·Kotlin/Java compile·py_compile·php -l) 삭제 + 나머지 14개를 `static-checks.js` 단일 스크립트로 통합(입력 1회 읽고 확장자 분기, `console.log(d)` 전량 제거, 출력 `hookSpecificOutput.additionalContext`). matcher 를 도구명 정규식 `Edit|Write|apply_patch` 로, 경로필터는 스크립트 내부. **발화 실측 성공** — 별도 헤드리스 세션에서 `println` .kt / `var_dump` .php 편집 시 경고 출력, 깨끗한 파일은 무출력, `./gradlew` 미실행. 사용자 결정: 검사만 살리고 자동실행 제외.
- **2-A** `~/.codex/sops` 심링크 + `AGENTS.tail.md` 에 SOP 목차 6건. AGENTS.md 19,606B(상한 32,768B).
- **2-B** `install-sync-check.sh` → `shared/hooks/core/` + 코어계약 + `case` 3경로 확장 + 검사 3종(CLAUDE-*·shared/hooks/core↔manifest·codex/hooks↔hooks.json) + 3도구 배선. 부수발견: `DEPRECATED_CLAUDE_LINKS` 영구 오탐 제거.
- **2-C** `drift-lib.sh` 에 `codex_drift_reasons()` 이관 → codex drift-check 얇은 래퍼화 · `smoke-hooks.sh` Codex/agy 대상 추가 + **timeout 폴백(perl)** → rc=127 15건 소멸 · `test.sh` codex shim 3케이스.
- **2-D** `CLAUDE-analysis-cache.md` 는 `sop_double_cache.md` 와 거의 전량 중복 → 고유분(디렉터리 트리)만 SOP 병합, 포인터로 교체. `sop_work_log.md` 의 "activity-logger 자동 append" 허위 약속 정정(수동이 정본).
- 메인 추가 수정: `.codex/hooks.json` 의 dangling 참조(이동된 install-sync-check.sh) → `shims/codex.sh install-sync-check` 로 교체 + matcher 에 `apply_patch` 추가.
- VERIFY: `test.sh` **PASS=22 FAIL=0**(16→22) · `smoke-hooks.sh` **26건 실패 0**(베이스라인 15건 전건 rc=127) · `install.sh personal` ❌0·2회차 멱등 · `drift-report` 드리프트 없음·shim 배선 7건 · `codex --profile routine mcp list` exit 0 · Stop 훅 `grep -c` 양쪽 **0 유지**.

**노션 프로젝트 확인**: 어제(9/12) 생성분 = 🏠 집 정리 PRO-129(Planning·높음·개인/정리·9/12~10/31). 구조 완비·체크박스 전량 미완. 지적 2건 — ① 볼트 `01.Projects/집 정리` 폴더 없음(벌크업 PRO-128 도 동일) = 05.프로젝트.md 「양쪽에 함께 만든다」 위반 ② 후보·구매예정 링크 15개가 전부 `품목 확인 필요`. Status 는 산출물 근거상 Planning 유지(변경 안 함).

**사진 분석 가능 여부 회신**: Claude Code = `Read` 도구로 이미지 직독 / Codex = `codex -i <파일>`(`--help` 실측). 한계(가려진 것·서랍 안쪽·치수 추정 불가) 명시.

**종료**: 카페 작업 종료, 집에서 재개. NEXT-SESSION 최상단에 신규 인계 절(A 배치개선 / B 집정리 / C 이월) 작성. Phase 5·6·4 미착수. 커밋·푸시 0건.
