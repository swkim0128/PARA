# 2026-09-09 작업 로그

> 9/9 00시대 P0·P1·P2-1~3 기록은 [[.claude/work-log/2026-09-08]] 말미에 "이어서" 절로 있다. 이 파일은 01:00 이후 재개 세션.

## P2-4 완료 — Codex 드리프트 훅 생성물 해시 비교 (`vibe-ai-config` `3212054`)

- `vibe-ai-config` 는 cwd 밖이라 `vibe delegate vibe-ai-config` pane(%18)에 위임. 메인은 지휘·기록만.
- `codex/hooks/config-drift-check.sh`: 기존 "존재 여부"만 보던 `hooks.json`·`vibe.rules` 에 더해 **생성물 3종을 정본에서 메모리 재생성 → sha256 앞 12자 비교**.
  - AGENTS.md = `install_codex_agents` 와 동일 concat(헤더 1줄 + 조각마다 빈 줄 선행) · vibe.rules = `python3 gen-rules.py settings.base.json` · hooks.json = 단순 복사 sha.
  - 재생성 로직은 `install.sh` 안에 복제(공유 함수로 뽑지 않음 — 단순화 우선, 주석으로 install.sh 위치 명시). **install.sh 생성 규칙을 바꾸면 이 훅도 같이 고쳐야 오탐이 안 난다.**
  - jq·shasum·python3 없으면 해당 항목만 조용히 건너뜀.
- VERIFY(위임 pane 보고): shellcheck 클린 · 현 배포본 대조 무출력 · 배포본 3종 각각 교란 → 각각 경보 · 정본 `AGENTS.tail.md` 교란 → 경보 → 원복 후 무출력.
- `docs/settings-inventory.md` 자동화 표에 "드리프트 경보(Codex)" 행 추가(기존 행은 Claude 로 구분).

## 발견·수정 — Claude 쉼의 deny 가 턴 전체를 죽였다 (`efacc0c`)

- 위임 pane 이 `bash-chain-guard` 에 걸릴 때마다 **`PreToolUse:Bash hook stopped continuation`** 으로 턴이 끊겨 재주입을 3회 반복해야 했다.
- 원인: `shared/hooks/shims/claude.sh` 가 `deny` → `{"continue":false,"stopReason"}` 로 번역. 이건 "그 도구 호출 차단"이 아니라 **턴 중단**이라, 에이전트가 명령을 분해해 재시도할 기회가 없다. Codex 쉼은 9/8 부터 `permissionDecision:"deny"` 로 올바르게 내고 있었다(Codex 가 `continue:false` 를 거부해서 고쳤던 것 — Claude 쪽은 "받아주니까" 그대로 남아 있었다).
- 수정: `EVENT=pre_tool` 의 deny/ask → `hookSpecificOutput.permissionDecision`(codex.sh 와 동일). `continue:false` 는 도구 단위 차단 수단이 없는 `prompt_submit`·`stop` 전용으로 축소. `test.sh:56` 옛 계약 단언을 새 형식으로 갱신(같은 커밋).
- VERIFY: shellcheck 클린 · `shared/hooks/test.sh` PASS=13 FAIL=0.
- `shared/hooks/` 는 심링크라 **Claude 쪽은 즉시 라이브**. 이 세션 자체도 위임 대기용 `until … ; do sleep; done` 한 줄이 가드에 걸렸다 → 스크래치에 `wait-pane.sh` 스크립트 파일로 대체(정당한 우회: 규칙은 일회성 체이닝 명령이 대상).

## 세션 종료 처리

- `vibe-ai-config` master ↑2(`3212054`·`efacc0c`) 푸시 — 업무 컴이 받아야 하므로 사용자 지시("업무컴에서 이어나갈 수 있도록")에 따라 푸시.
- para: `NEXT-SESSION.md` 상단 갱신 + 이 로그 + 누적 미커밋 8건 → `para-work backup`.
- 위임 pane %18 프롬프트에 `install.sh work 재실행해서 배포 반영해` 가 **미전송 상태**로 남아 있었다. 이 머신 env 는 `personal`(`~/.claude/.vibe-install-state`) — `work` 로 돌리면 오버레이가 갈린다. 전송하지 않고 사용자에게 보고.

## 추가 — install.sh personal 직접 배포 (01:24)

- 위임 pane %18 Claude TUI 가 입력·Escape·SIGWINCH 전부 무반응(프로세스 생존, MCP 자식만). 미전송 `install.sh work` 문구는 C-u 로도 지워지지 않아 무시.
- 메인에서 `install.sh personal` 단일 명령 실행: settings.json 재생성(백업 `.bak.20260909-012448`) · Codex AGENTS.md 15,023B · hooks.json · rules 105건 · config.toml(백업 `.bak.20260909-012452`). install-state `head=efacc0c`.
- VERIFY: `codex/hooks/config-drift-check.sh` 실행 → 무출력(배포본 == 정본).
- `status/personal.md` 갱신 커밋 `2a29cdf` 푸시. vibe-ai-config master == origin.
