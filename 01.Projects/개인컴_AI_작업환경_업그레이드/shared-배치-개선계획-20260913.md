# vibe-ai-config 공용/전용 자산 배치 + 플러그인 개선 계획 — 2026-09-13

> 근거 조사(전부 읽기 전용): ① 훅 축·② 비훅 자산 축(Explore, Fable 세션) · ③ 플러그인 A축(tmux-suite·git-suite·dev-suite·analyze·notify)·④ 플러그인 B축(work-suite·task-mgmt·personal-suite·google-workspace·vibe-admin-mcp·`_unused`·marketplace) — ③④는 **Opus** 로 수행. 선행 문서 [[codex-이식기능-감사-20260913]] · [[codex-claude-동등설정-계획]] §4-1.
> 이 문서는 **계획**이다. 아직 아무것도 옮기지 않았다. 각 단계는 `[작업] → verify: [정적 도구 단일 명령]` 으로 닫는다. "미검증"이라 적힌 판정은 파일 존재·등록만 확인한 것이고 실행은 안 해봤다는 뜻이다.

## 0. 배치 규율 (판정 기준)

| 구분 | 위치 | 정의 |
|---|---|---|
| 공용 | `shared/` | Claude·Codex(·agy) **둘 이상**이 소비. 도구 차이는 `shared/hooks/shims/*` 가 흡수 |
| Claude 전용 | `claude/` | Claude 이벤트·도구(`Task`·`WorktreeCreate`·`Notification`·`EnterPlanMode`·`transcript_path`)에 묶이거나 Claude 만 배선 |
| Codex 전용 | `codex/` | `config.toml`·`hooks.json`·rules·`AGENTS.tail.md` 등 Codex 생성물의 정본 |
| 정본 홈(예외) | 루트 `skills/` | `npx skills add "$REPO_ROOT"` 규약상 루트 스캔. **실질 shared** — §1-D 에서 명문화 |

**총평** — `shared/` 구조(core+shims·rules·commands·sops)는 규율 성립. 위반은 전부 **"공용인데 `claude/` 밑"** 한 방향(Claude 선행 → Codex 가 나중에 같은 자산 소비 → 자산이 이사 안 함). 플러그인은 여기에 더해 **"플러그인 포장이 실제 소비 경로와 무관한" 케이스**가 여럿 드러났다(§4 참조 — 소비자가 레포 절대경로를 직접 읽고 캐시 사본은 죽어 있음).

## 1. Phase 1 — P0: 참조 단일 지점 · 저위험 ✅ **2026-09-13 완료 (미커밋)**

> 전 단계 게이트 통과·롤백 0·건너뜀 0. 브랜치는 `master`(계획서가 `main` 으로 적었던 것 정정).
> 메인 독립 재확인: `git status` 변경 범위 · Codex shim 실측(SessionStart 입력 + `VIBE_DELEGATED=1` → 억제문구 / env 없으면 무출력) · `drift-report.sh --run` → `드리프트 없음`·`shim 배선 6건`(5→6) · `~/.agents/skills` 구 경로 0건.
> 🔴 **사용자 단계 남음**: Codex TUI `/hooks` 에서 신규 `delegate-pane-guard` 항목 `t` 재신뢰(`codex/hooks.json` 변경분은 신뢰 전까지 발화 안 함).
> 실행 중 추가로 손댄 파일(게이트 충족에 필요): `docs/architecture-audit.md`(사문 `claude-callback.sh` 예시 → 실재 `claude-send.sh`) · `docs/settings-inventory.md`(mcp.base.json 경로 2행) · `skills/harness/claude-md-setup/SKILL.md`(해제된 훅 설명 정정).
> 신규 발견(→ Phase 2 편입): `shared/sops/sop_work_log.md` L7·95 가 `activity-logger.sh` 자동 append 를 약속하는데 그 훅은 8/31 해제·이번에 `_unused/` 로 이동 — 폐경로(`vibe-claude-plugin/claude-config/hooks/`)까지 적혀 있어 **하지 않는 자동화를 약속하는 SOP** 상태.



### ✅ 1-A. `claude/mcp.base.json` → `shared/mcp.base.json`
- 근거: `_note` 자기선언 "MCP 정본(도구 무관)". `install_codex_mcp()` 가 `jq` 로 `.mcpServers`+`.codexOnly` 파싱 → `codex mcp add`.
- 변경: `git mv` → `install.sh` `BASE_MCP` 베이스를 `$REPO_ROOT/shared` 로 → `codex/hooks/config-drift-check.sh` 가 읽는 경로 동시 수정.
- verify: `grep -rn "mcp.base.json" vibe-ai-config --exclude-dir=.git` 전부 `shared/` · `./install.sh personal` ❌ 0 · `codex mcp list` == 정본 · `drift-report.sh --run` 무출력.

### ✅ 1-B. `claude/hooks/delegate-pane-guard.sh` → `shared/hooks/core/`
- 근거: stdin 파싱 없이 `VIBE_DELEGATED=1` env 만 보고 `additionalContext` 출력 — shim 이 번역하는 그 형태. **실동작 구멍**: Codex pane 위임 시 억제 지시 미주입.
- 변경: 이동 + `{verdict,reason,context}` 계약 → `manifest.json` 등록(`tools:[claude,codex,agy]`) → `settings.base.json` 배선을 `shims/claude.sh` 경유로 → `codex/hooks.json` SessionStart 추가 → agy 재생성 → `install.sh` 훅 배열·`install-sync-check.sh` 감시 목록.
- ⚠️ `codex/hooks.json` 변경 → 해당 항목 조용히 미신뢰 → §8 사용자 단계.
- verify: `jq . codex/hooks.json` · `bash shared/hooks/test.sh` · `VIBE_DELEGATED=1 bash shared/hooks/shims/codex.sh delegate-pane-guard </dev/null` 출력 확인 · `smoke-hooks.sh --run`.

### ✅ 1-C. 사문 훅 → `claude/hooks/_unused/`
- `activity-logger.sh`(8/31 해제, `core/activity-log.sh` 와 중복. `docs/settings-inventory.md` 보존 사유 있음 → **삭제 아님**) · `claude-md-capture-reminder.sh`(같은 날 해제, 유일 참조 `skills/harness/claude-md-setup/SKILL.md` 문구 정정).
- verify: `grep -rn "activity-logger\|claude-md-capture-reminder" --exclude-dir=.git` 이 `_unused`·docs 만.

### ✅ 1-D. `shared/skills/` 빈 디렉터리 제거 + 문서 정정
- 루트 `CLAUDE.md` 트리가 `shared/skills/` 를 SSoT 라 적어 **실제(루트 `skills/`)와 반대**. `rmdir` + 트리 정정 + "npx 규약상 루트 스캔 — shared 동급 정본" 1줄.
- verify: `test ! -d shared/skills` · `grep -n "shared/skills" CLAUDE.md shared/README.md` 0건.

### ✅ 1-E. 🔐 `vibe-admin-mcp` API 키 파일 gitignore (즉시)
- 근거: `outline-mcp-server/src/utils/client.ts` 가 키를 `${CLAUDE_PLUGIN_ROOT}/config.json` 에서 읽는데 루트·서브 `.gitignore` 어디에도 `config.json` 없음 → 키 생성 시 **커밋 가능 상태**(현재 파일 미존재 확인).
- 변경: 루트 `.gitignore` 에 `claude/plugins/vibe-admin-mcp/config.json` 추가. 키 설정 절차 문서가 `_unused/vibe-admin/commands/outline-setup.md` 에만 고립 → `vibe-admin-mcp/commands/outline-setup.md` 로 회수(§6-C).
- verify: `git check-ignore -v claude/plugins/vibe-admin-mcp/config.json` 히트.

### ✅ 1-F. 문서 사문 참조 정정
- 루트 `CLAUDE.md` SSoT 목록의 `claude-callback.sh` — **실재하지 않음**. `marketplace.json` git-suite 설명 "자동 commit·push 훅(Stop)" — hooks.json 에 Stop 훅 없음(Pre/Post 만). `shared/hooks/README.md` 「배선 상태」(보류·ask 모드 → 실제는 배선·deny).
- verify: `grep -rn "claude-callback" vibe-ai-config --exclude-dir=.git` 0건 · `grep -n "Stop" .claude-plugin/marketplace.json` 0건.

## 2. Phase 2 — P1 ✅ **2026-09-13 완료 (미커밋)**

> 2-A `~/.codex/sops` 심링크 + `AGENTS.tail.md` SOP 목차 · 2-B `install-sync-check` 코어 승격 + 검사 3종으로 실질화 · 2-C `drift-lib` 에 `codex_drift_reasons()` 이관·smoke 에 Codex/agy 대상 추가·**`timeout` 폴백(perl)로 rc=127 15건 소멸**·test 에 codex shim 3케이스 · 2-D `CLAUDE-analysis-cache.md` 를 SOP 포인터로 축약 + `sop_work_log.md` 의 허위 자동화 약속 정정.

### 원래 계획 (이력)

### 2-A. `shared/sops/*` Codex 배포
- `~/.codex/sops` 부재, Codex 지침에 `sops` 0건. `INSTALL_LINKS` 에 `$CODEX_DIR/sops|shared/sops` 추가 + `codex/AGENTS.tail.md` 에 SOP 경로 목차(Codex 는 `@import` 없음 → 꼬리에 적어야 로드). 32 KiB 상한 재확인.
- verify: `test -L ~/.codex/sops` · `install_codex_agents` 바이트 < 32768.

### 2-B. `install-sync-check.sh` → `shared/hooks/core/` + `case` 를 `claude/`·`codex/`·`shared/` 로 확장 + `codex/hooks.json` PostToolUse(`apply_patch`) 배선.
- verify: `test.sh` 에 "codex 가 `codex/hooks/x.sh` 편집 → 경고" 케이스.

### 2-C. 진단 3종의 Codex 커버리지
- `lib/drift-lib.sh` 에 `codex_drift_reasons()` 이관 → `codex/hooks/config-drift-check.sh` 얇은 래퍼화(헤더 규율 + `codex/README.md` "Claude 메커니즘 비의존" 충족, 1-A 와 함께 `claude/*.json` 직접 참조 소멸).
- `smoke-hooks.sh` 대상에 `~/.codex/hooks.json`·`~/.agents/hooks.json` 추가 + `timeout` 부재(15건 rc=127) → `gtimeout`/내장 폴백.
- `test.sh` 에 `CODEX_SHIM` 케이스 — pre_tool_use deny → `permissionDecision:"deny"`, 비-pre_tool deny → additionalContext 강등.
- verify: `test.sh` 전건 · `smoke-hooks.sh --run --with-contract` ❌ 0 · `drift-report.sh --run` 무출력.

### 2-D. `claude/CLAUDE-analysis-cache.md` ↔ `shared/sops/sop_double_cache.md` 내용 대조 → 중복이면 SOP 로 병합, Claude 파일은 포인터.

## 3. Phase 3 — 운영 결함 ✅ **2026-09-13 완료 (3-C 제외, 미커밋)**

> 3-A·3-B·3-D·3-E·3-F 수정 완료 · 3-C 는 **실측만** 수행(수정 보류 → 결정 §9-5).
> 메인 독립 재확인: Stop 훅 `grep -c Stop` 양쪽 0 · HEAD `c99a80d` 유지(커밋 0) · `test.sh` PASS=16 FAIL=0 · `git-guard.sh` 가 `permissionDecision:"deny"` 사용.
> **shellcheck 개선**: 베이스라인 SC2009 3건 → 1건(신규 0, 2건은 3-E 수정으로 소멸).
>
> **🔴 3-C 실측 결과 — `analyze` PostToolUse 19개는 발화하지 않는다(확정).**
> 프로브: 작업 디렉터리 밖에 `settings.gradle` + 실행 시 마커를 남기는 **가짜 `gradlew`** + `Probe.kt` 배치 → Edit 도구로 실제 수정 → **마커 미생성**. 양성 대조군으로 같은 훅 명령을 직접 실행하면 마커 생성 + `[Hook] ktlintFormat applied` 출력 → 스크립트는 정상이고 **차이는 발동 여부뿐**. 보조 근거: `println` 포함 `.kt` Edit 후 트랜스크립트에 `[Hook] WARNING: println() found` 0건. `analyze` 플러그인은 `enabledPlugins` 에 활성 상태.
> **`console.log(d)` 는 19/19 전부** 훅 입력 JSON 전체를 stdout 에 되뱉는다.
> → 즉 matcher 표현식 문법은 Claude Code 가 해석하지 않는다. 고치면 **19개가 갑자기 발화하기 시작**하고, index 0 은 Kotlin 편집마다 `./gradlew ktlintFormat` 을 **실제로 실행**한다. 결정 §9-5 참조.
>
> **판정 반전 1건**: 3-E 조사에서 "중복 pane 가드가 Codex 에만 무효" 라 했으나, `pane_current_command` 가 claude pane 을 `2.1.265`(버전 문자열)로 잡아 **Claude 에서도 이미 무효**였다. 이제 ps 기반 `pane_tool_name()` 으로 양쪽 동작한다.
>
> **부수 효과 1건**: 3-F 검증으로 `auto-retrospect.sh` 를 실행해 `~/.cache/personal-suite/last-sunday-nudge` 가 갱신됐다 — 9/13(일) 세션 종료 시 주간 루틴 안내가 안 뜰 수 있다.
>
> **잔여 한계 1건**: `apply_patch` 의 `tool_input` 필드명이 미문서화라 `task-status-done-reminder.sh` 의 `file_path` 추출 로직은 손대지 않았다(matcher 는 매치되나 필드명이 다르면 pass-through). 추측 대신 Codex 실측 후 수정.
>
> **§7-A 복원 조건 충족**: 3-D 가 처리돼 Stop 훅을 되살려도 남의 staged 변경을 쓸어담지 않는다.

### 원래 결함 표 (이력)


| # | 결함 | 실측 근거 | 조치 | verify |
|---|---|---|---|---|
| ✅ 3-A | `readonly-bash-allow.sh` ↔ `bash-chain-guard` 충돌 — **deny 가 allow 를 이김** | 이 세션 `codex … \| head`·조사 에이전트 `find … \| sort` 차단 | **결정 필요(§9-1)**. 권고: chain-guard 정본(AGENTS.md 절대금지 + 9/9 결정 1번), allow 훅의 파이프 패턴 제거 | `test.sh` 에 `grep x \| head` 케이스 |
| ✅ 3-B | `git-suite/hooks/git-guard.sh` 가 `{"continue":false,"stopReason"}` 출력 | `shims/codex.sh` 주석 "PreToolUse 에서 이 형식은 `hook: PreToolUse Failed` 로 죽는다(9/8 실측)". Codex `hooks.state` 등록됨 = 실제로 발동 대상. shim 을 안 거치는 유일한 가드형 플러그인 훅 | `hookSpecificOutput.permissionDecision:"deny"` 로 교체(Claude 도 수용 — `analyze/php-encoding-check.sh` 가 이미 이 형식으로 양쪽 동작) | Codex 세션에서 `git push --force` 시도 → deny 메시지 · `jq . hooks.json` |
| 🔍 3-C (실측완료·수정보류) | `analyze` PostToolUse 19개 matcher 가 `tool == "Edit" && tool_input.file_path matches "\.kt$"` 표현식 | Claude matcher 는 도구명 정규식 → **19개 전부 미발화 가능**. 레포 문법 근거 0건. 각 훅 `console.log(d)` 가 입력 JSON 전체를 stdout 재출력 | ① 스크래치 `.kt` 파일 Edit 로 발화 실측 ② 미발화면 matcher 를 `Edit\|Write\|apply_patch` + 스크립트 내부 `file_path` 필터로 교체, `console.log(d)` 제거 ③ 19개 중 실사용 스택(Kotlin·Python·PHP)만 남기고 정리 | 발화 실측 로그 · `jq '.hooks.PostToolUse \| length'` |
| ✅ 3-D | ~~`auto-version-bump.sh`(Stop) 미작동~~ **정정: 작동한다.** 2026-09-13 실측 — Codex pane 종료 때마다 발화해 자동 커밋 3건 + push 시도(사고, work-log 참조). 진짜 문제는 ① 미작동이 아니라 **stage 된 것만 커밋해 깨진 HEAD 를 만든다** ② 반복 발화로 8회 bump ③ 캐시 내용 불일치는 별개 원인 | tmux-suite `1.0.73` 이 레포·Claude 캐시·Codex 캐시 **번호 동일·`vibe.sh` 내용 셋 다 다름**. 16:25 커밋 건에서 patch 미상승. Codex 는 `.codex/hooks/auto-version-bump.sh` 절대경로 별도 사본 | 원인 추적(Stop 훅 발화 여부·`git status` 판정 조건·Codex 사본 drift) → 두 사본 통일 → 수동 bump 1회로 캐시 재동기화 | `diff -rq claude/plugins/tmux-suite ~/.claude/plugins/cache/swkim0128/tmux-suite/<ver>` 무차이 |
| ✅ 3-E | `tmux-suite/scripts/lib/pane-state.sh` 가 `ps … grep "[c]laude"` 로만 유휴 판정 | **Codex pane 은 영원히 "유휴"** → `pane-wait.sh` 즉시 반환·`vibe.sh` 중복 pane 가드 무효. 이름에 `claude` 가 없는데 실제 Claude 전용인 케이스 | `[c]laude\|[c]odex\|[g]emini` 로 확장 또는 pane title 의 `[tool]` 마커 기준 판정. `vibe.sh` `--ro`·중복가드·`resume` 3블록도 `--tool` 분기 | `test-vibe-delegate.sh` 에 codex pane 대기 케이스 |
| ✅ 3-F | `task-mgmt` 훅 matcher `Edit` ≠ Codex `apply_patch` · `personal-suite`/`task-mgmt` 훅 출력 `systemMessage` 가 Codex 수용 스키마 밖 | `manifest.json` `tool_name_map.edit.codex="apply_patch"` · `codex.sh` 수용 = `permissionDecision`/`additionalContext` 만. 등록은 1:1 정확 | matcher `Edit\|Write\|apply_patch` · 출력을 `hookSpecificOutput.additionalContext` 로 통일(양쪽 수용). **플러그인 훅 출력 규약을 §5-B 로 명문화** | Codex 세션 실측 1회 |

## 4. Phase 4 — 플러그인 배치 재설계 ✅ **2026-09-13 완료 (옵션 A', 미커밋)**

> 도구 무관 스크립트 14개 → `shared/scripts/{tmux-suite,git-suite}/`. Claude 전용 3종(`claude-switch.sh`·`reap-idle-claude.sh`·`claude-skills.sh`)은 플러그인 잔류. `marketplace.json` `source`·`plugin.json` `version` 미변경(포장 유지).
> 경로 참조 12곳 갱신 · `vibe.sh` 에 `vibe_resolve_script()` 신설 · preamble 폴백 5단→4단 · `tmux-suite/install.sh` 를 두 소스 디렉터리 훑도록 재타깃 + **끊어진 구 심링크 자동 정리** + `--links-only` 신설 후 루트 install 편입(`~/.zshrc` 를 건드리는 전체 모드는 수동 유지).
> **부수 수정**: `skills/session/tmux-session-comm/scripts/get-tools-and-targets.sh` 의 `PLUGIN_ROOT` 가 이미 깨져 있던 것 정정.
> 메인 독립 재확인: `readlink -f ~/.local/bin/vibe` → `shared/scripts/tmux-suite/vibe.sh` · 새 경로 `pane-list.sh` 정상 · 배포된 `~/.claude/shared-commands.md` 새 경로 반영 · 심링크 dangling 0 · **왕복 실측**(para pane %8 → `phase4-ok` 응답 후 정리).

### 원래 계획 (이력)

### 4-0. 새로 드러난 사실 — "플러그인 포장이 실제 소비 경로와 무관"
| 사실 | 근거 | 함의 |
|---|---|---|
| `tmux-suite` 실사용은 `~/.config/vibe-tools/*` 심링크가 **레포를 직접** 가리킴 | `vibe.sh -> …/vibe-ai-config/claude/plugins/tmux-suite/scripts/vibe.sh` | 플러그인 캐시 사본은 죽은 복사본. `delegation-preamble.sh` 5단 `..` 가 캐시 위치에선 `shared/` 못 찾아 **양쪽 캐시 fail-open** |
| `references/plane-common.md`·`notion-common.md` 소비자(스킬 8건)가 **레포 절대경로** `~/Project/vibe-ai-config/claude/plugins/…` 를 읽음 | `~/.agents/skills/{task-intake,task-complete,multi-dispatch,notion-diary,…}/SKILL.md` | 캐시 사본 4벌 사문. **디렉터리를 옮기면 스킬 8건이 깨진다** — 이동 전 선행 조치 필수 |
| `tmux-suite/install.sh`·`git-suite/install.sh` 는 루트 `install.sh` 가 호출하지 않음 | `grep tmux-suite/install.sh install.sh` 0건 | 수동 실행 전용. git-suite 것은 일회성 마이그레이션인데 **미실행 상태**(`~/.config/vibe-tools/git-template-config.json` 잔존) |
| `google-workspace` 는 껍데기 | 2파일 중 `gws-quickref.md` 참조 0건(사문), `cal-to-notion.md` **frontmatter 없음**. 실동작은 전부 `~/.agents/skills/gws-cal-to-notion/` | 플러그인 폐지 후보 |
| `vibe-admin-mcp` 는 유일한 Claude 전용(업무머신 overlay) | `settings.work.overlay.json` 만 · Codex 미등록 | `claude/plugins/` 잔류 정당 |

### ✅ 4-1. 선행 디커플링 — **2026-09-13 완료(Phase 1 과 함께)** (Phase 1 과 함께 — 이동 여부와 무관하게 이득)
- `delegation-preamble.sh` 로더: 5단 `..` → `git -C "$dir" rev-parse --show-toplevel` 또는 `VIBE_AI_CONFIG_ROOT` env + 정본 미발견 시 **fail-open → stderr 경고**.
- `references/*.md` → **`shared/references/`** 로 이동 + 스킬 8건 참조 경로 갱신(스킬 정본은 레포 `skills/`, 배포는 `install.sh` 재실행). `notion-common.md` 는 볼트 `adapters/notion/README.md` 와 **SoT 선언 충돌**(둘 다 정본 자칭, `plugin.json` 은 볼트가 정본이라 명시) → §2.1·§2.2·§7 중복분은 볼트로 흡수, §4·§5·§6 고유분만 존치.
- verify: `bash claude/plugins/tmux-suite/scripts/test-vibe-delegate.sh` · `grep -rn "claude/plugins/.*/references" ~/.agents/skills` 0건.

### 4-2. 선택지 (결정 §9-2)
| 옵션 | 내용 | 비용 | 규율 |
|---|---|---|---|
| **A' (권고)** | 도구 무관 스크립트 묶음(`tmux-suite/scripts`·`git-suite/scripts`)을 **`shared/scripts/<suite>/`** 로 이동, `~/.config/vibe-tools/` 심링크·`~/.local/bin/vibe` 재타깃(`tmux-suite/install.sh`). 플러그인은 훅·커맨드만 남기고 `claude/plugins/` 잔류(Claude 규격 포장 + Codex 부분소비 사실을 README 에 명문화) | 심링크 12개 + 루트 `CLAUDE.md`·`CLAUDE-delegation.md` 경로 6곳 + `install.sh` 연결 | 스크립트=충족 · 플러그인 포장=문서화로 해소 |
| A | `claude/plugins/` → 루트 `plugins/` 통째 이동 | A' + `marketplace.json` 10건 + 양쪽 재설치 + 푸시 필수 | 충족 |
| B | 위치 유지·문서만 | 0 | 위반 지속 |
- A' 근거: 실소비가 심링크→레포 직결이라 플러그인 캐시는 사문(4-0). 포장을 옮기는 것보다 **실체(스크립트)를 shared 로 옮기는 게 규율의 실질**. 마켓플레이스 원격 재설치 없이 끝나 롤백 창이 좁다.
- verify: `bash shared/hooks/smoke-hooks.sh --run` · `vibe delegate para --tool codex "echo ok"` 후 `pane-peek.sh <pane> --grep "확인·승인 요청 금지"` 1건 · `which vibe` → `shared/scripts/…`.

## 5. Phase 5 — 플러그인 구성요소 Codex 호환 ✅ **2026-09-13 완료 (미커밋)**

> 5-B 규약 명문화 + **`test.sh` 린트로 결정론적 강제**(위반 파일 임시 생성 → FAIL 실측 후 원복) · 5-A 매핑 표 8건 + `cal-to-notion.md` frontmatter + `todo.md` EnterPlanMode 분기 · 5-C 에이전트 4종 Claude 전용 확정(**브리핑 「이식 에이전트 실동작 검증」 종결**) · 5-D Notification 비대칭 문서화(코드 변경 0).
> `~/.codex/AGENTS.md` = 22,448B (상한 32,768B).

### 원래 계획 (이력)

### 5-A. 커맨드 — Codex 노출 실태
Codex 는 `commands/*.md` 를 슬래시로 노출하지 않고 설치 시 `.codex-plugin/migrated-command-skills/source-command-<name>/SKILL.md` 로 변환. **8개 커맨드 중 3개만 변환**(`todo`·`commit`·`analyze`), `work`·`personal`·`notion`·`cal-to-notion`·`mr`·`code-review`·`dev` 미변환. 캐시 mtime 동일·frontmatter 동일이라 시점·스키마 가설 모두 반증 — **원인 미규명**.
- 조치: ① Codex 버전·변환 규칙 확인(`codex --version`, 플러그인 문서) ② 디스패처형 커맨드(`/work`·`/personal`·`/dev`)는 부르는 스킬이 `~/.codex/skills → ~/.agents/skills` 로 **전부 도달 가능**하므로 `codex/AGENTS.tail.md` 에 "Codex 에선 스킬 이름 직접 호출" 매핑 표 ③ `/todo` 는 1단계 `EnterPlanMode` 강제 → Codex 분기(plan 모드 생략) ④ `cal-to-notion.md` frontmatter 추가.
- verify: Codex 세션 `/skills` 목록 실측 1회 · `head -3 commands/cal-to-notion.md` 가 `---`.

### 5-B. 플러그인 훅 출력·매처 규약 명문화 (3-B·3-F 의 정본화)
- 규칙: 플러그인 `hooks.json` 은 양 도구가 **같은 파일**을 읽으므로 shim 을 못 거친다 → ① matcher 는 `Edit|Write|apply_patch` 처럼 양쪽 도구명 OR ② 출력은 `hookSpecificOutput.{permissionDecision,additionalContext}` 만(`continue:false`·`systemMessage` 금지) ③ 준수 예시 = `analyze/hooks/php-encoding-check.sh`.
- 위치: `shared/hooks/README.md` 「플러그인 훅 규약」 절 + `test.sh` 에 플러그인 hooks.json 스키마 린트(`jq` 로 `continue`·`systemMessage` 키 검출).
- verify: `bash shared/hooks/test.sh` 린트 케이스 통과.

### 5-C. `work-suite/agents/*` 4종 — Claude 전용으로 확정·문서화
- frontmatter `model: sonnet`·`Agent(run_in_background=True)`·`tools:` 가 Claude 도구명. `~/.codex/AGENTS.md` 가 위임 전략을 `CLAUDE-delegation.md` 로 위임. Codex 등가는 내장 조사 서브에이전트(감사 문서에서 실사용). → **이식 대상 아님**으로 확정, `codex/AGENTS.tail.md` 에 "에이전트 4종 ↔ Codex 등가 수단" 1줄. 브리핑 3번("이식 에이전트 4개 실동작 검증")은 이걸로 종결.

### 5-D. `notify` Notification 8개 — 구조적 비대칭, 조치 없음
- Codex 에 `Notification` 이벤트 없음(shim 이벤트 case 도 5종). Codex 는 최상위 `notify = […,"turn-ended"]` 로 이미 대체. `cmux-notify.sh` 는 내용 도구 무관이나 Codex 호출 경로가 없다 → Codex `notify` 훅에서 `cmux-notify.sh` 를 부르는 방안만 검토(선택).

## 6. Phase 6 — 정리 ✅ **2026-09-13 완료 (미커밋)**

> 회수 3건(`secret-write-guard`·`destructive-bash-guard`·`outline-setup.md`) · 사문 삭제 10건 · outline allow 10개 base→work overlay · `git-suite` 마이그레이션 실제 수행 + 루트 install 편입 · `_unused/` 거버넌스 신설.
> **원본 대비 의도적 수정 2건**: ① `secret-write-guard` 가 필드명 고정 대신 `args` 전체 문자열 리프 스캔(Codex `apply_patch` 스키마 불확실) ② `destructive-bash-guard` 치명경로 판정을 루트/홈/상위 **정확 매치**로 축소(원본은 모든 절대경로 오차단).
>
> **🔴 회수 직후 오탐 발생 → 수정 완료**: `destructive-bash-guard` 가 heredoc 본문의 **문서 텍스트**를 명령으로 오판해 메인의 작업 로그 작성·검색·서술 명령을 전부 차단했다(실제 발생). 공통 전처리를 **`shared/hooks/lib/cmd-lib.sh`** 로 추출해 `bash-chain-guard` 와 **같은 파일 하나를 source**. 따옴표 처리를 세그먼트 명령어별 분기 — 텍스트·검색 명령은 인자 제거, 파괴 후보는 따옴표만 벗겨 대상 보존(그 결과 `rm -rf "$HOME"` 이 오히려 정확히 치명경로로 잡히고 `mysql -e "DROP TABLE x"` 도 새로 잡힘). UNCERTAIN 정책은 비대칭(chain=deny 유지 / destructive=차단 안 함). 회귀 8건 추가 → `test.sh` **PASS=38**.
> 남은 한계(명시): 따옴표로 감싼 명령치환 미탐 · 텍스트 명령 인자 전면 제거의 대가 · heredoc 탐지가 따옴표 미존중(chain-guard 기존 한계 이식) · 파괴 후보 화이트리스트 밖 래퍼.

### 원래 계획 (이력)

### 6-A. 사문 삭제
`google-workspace/references/gws-quickref.md`(참조 0) · `notify/prompts/_unused/07`(로더 `-mindepth 5 -maxdepth 5` 라 depth 6 영구 배제) · `git-suite/install.sh`(일회성 마이그레이션 — 실행 후 삭제 또는 루트 `install.sh` 에 편입) · `settings.base.json` 의 `mcp__plugin_vibe-admin-mcp_outline__*` allow 10개 → `settings.work.overlay.json` 으로 이동.

### 6-B. 유령 설치 청소 (사용자 단계 포함)
`~/.claude/plugins/installed_plugins.json` 에 마켓 미등재 `harness 3.2.13`·`test 1.1.5`·`vibe-admin 2.1.6` 잔존 + 캐시 디렉터리 · `work-suite/{1.0.2,1.0.3}` 고아 캐시(`.orphaned_at` 有) · Codex 캐시의 `prompts/*` 6개·`references/*` 사본(아무도 안 읽음 — 마켓 재설치 시 자연 소멸).
- verify: `jq '.plugins \| keys' ~/.claude/plugins/installed_plugins.json` 에 세 이름 없음.

### 6-C. `_unused/` 회수 후보 vs 폐기
| 회수 후보 | 이유 | 목적지 | 게이트 |
|---|---|---|---|
| `harness/hooks/write-guard.sh` | 시크릿 5종(AKIA/ghp_/glpat-/sk-/PRIVATE KEY) 차단. **현행 전체에 시크릿 스캔 부재** — 1-E 위험과 직결 | `shared/hooks/core/secret-write-guard.sh`(3도구 shim) | 신설이므로 `asset-intake` 3분기 판정 먼저 |
| `harness/hooks/bash-guard.sh` | rm -rf·DDL·`curl\|sh`·publish·chmod 777·infra destroy 차단. `bash-chain-guard` 는 체이닝만 봄 | `shared/hooks/core/destructive-bash-guard.sh` — **"개발서버 tmux 강제"·"worktree 런타임 차단" 2규칙은 제외**(cmux 워크스페이스 전제와 충돌) | 동상 + 결정 §9-6 |
| `vibe-admin/commands/outline-setup.md` | 활성 `vibe-admin-mcp` 의 유일한 키 설정 절차 | `vibe-admin-mcp/commands/` (1-E) | — |
| `harness/commands/review-mr.md` | GitLab MR diff+인라인 코멘트 — 현행 `code-review` 는 GitHub PR 만 | `skills/*/code-review` 에 `--mr` 분기로 흡수 | 사내 GitLab 사용 실태 확인 |
| `harness/hooks/pre-compact-checkpoint.sh` | `PreCompact` 를 쓰는 현행 훅 0건 | 검토 후 `shared/hooks/core/` 또는 폐기 | — |

**폐기(9)**: `write-audit.sh`(→`activity-log`) · `session-start.sh`(→`briefing-inject`) · `php-review.md`(스킬이 상위집합) · `handoff.md`(→`ctx-save`) · `harness-init.md`(→`claude-md-setup`) · `feature-development.md`(`pipeline` 스킬 부재·깨짐) · `test/tdd.md`(`tdd-workflow` 부재·깨짐) · `test/e2e-chrome.md`(`chrome-devtools-mcp` 가 대체) · `vibe-admin/harness-audit.md`(HANDOFF.md 가 이미 오탐 기록).
- 회수 완료 후 `_unused/` 디렉터리 제거 + 루트 `CLAUDE.md` 에 **`_unused` 거버넌스 1줄**(현재 규칙 0건 — 마켓 미등재·bump 스킵이라 조용히 썩음).
- verify: `test ! -d claude/plugins/_unused` · `git grep -n "_unused" CLAUDE.md` 가 거버넌스 문장 1건.

## 7. 보류 (판단 갈림)

- **`claude/settings.base.json`**: Codex 는 `gen-rules.py` 생성 소스로만 소비, 파일은 100% Claude 스키마. 장기 `shared/permissions.json` 승격 가능하나 **파일 일부만 도구 무관** → 현 위치 유지.
- **`shared/hooks/lib/drift-lib.sh`**: 2-C 로 해소. **`settings-guard.sh`**: 2-B 로 실질 커버.
- **`google-workspace` 플러그인 폐지**: 실체가 스킬에 있어 폐지 가능하나 `enabledPlugins`·Codex `[plugins]`·마켓 3곳 제거 동반 → Phase 6 에서 결정.
- **`marketplace.sh`**(`install.sh` 호출 0) · **`engines/slack-bridge/`**(참조 0) — 생존 여부는 사용자만 안다(§9-7).
- **`outline-mcp-server/dist/stdio.mjs`** — 의도적 벤더링(`.gitignore` 에 `dist/` 없음, `node_modules` 부재로 재빌드 불가). `src/` 와 일치 여부 미검증 — 업무머신에서 빌드 대조.

## 6-Z. 🔴 Phase 6 잔여 — 사용자 판단 대기

1. **회수원 3개 삭제 여부** — `_unused/harness/hooks/{write-guard,bash-guard}.sh`·`_unused/vibe-admin/commands/outline-setup.md`. 회수 완료됐으나 원본 잔존. 지울 때 `_unused/harness/hooks/hooks.json` 이 이미 삭제된 스크립트를 참조하는 것도 같이 정리.
2. **유령 플러그인 제거**(사용자 단계) — `claude plugin uninstall` 은 1개씩만 받는다(실측): `harness@swkim0128` · `test@swkim0128` · `vibe-admin@swkim0128`.
3. **보류 4건**(미접촉): `review-mr.md`(사내 GitLab 실태) · `marketplace.sh` · `engines/slack-bridge/` · `google-workspace` 플러그인 존폐(+`gws-quickref.md`).
4. **관찰(미수정)**: `analyze/hooks/hooks.json` PreToolUse matcher 가 `Read|Edit|Write` 라 **`apply_patch` 누락** → Codex 에서 `php-encoding-check.sh` 영구 미발화. 업무컴 Codex 로 EUC-KR PHP 를 다룰 계획이면 고칠 것.

## 7-A. ✅ Stop 훅 복원 완료 (2026-09-13)

`.claude/settings.json`·`.codex/hooks.json` 의 `Stop` 블록 복원.
- **백업 통째 복원이 아니라 `Stop` 블록만 병합했다** — 백업 이후 두 파일 모두 바뀌었다(Phase 2 의 `install-sync-check` shim 배선, Codex 쪽 dangling 경로 수정 + `apply_patch` matcher). 통째로 덮었으면 그 수정이 날아간다.
- **가드 실측**(스크래치 레포로 사고 조건 재현): 무관한 staged 변경이 있으면 bump 는 워킹트리에만 반영하고 **commit·push 를 건너뛰며** 실행할 `git commit --only …` 를 출력. `HEAD` 불변·무관 파일 미커밋. 9/13 사고 재현 안 됨.
- 백업 `.phase1-bak` 2개 보존(삭제 판단은 사용자).

## 7-B. 🔴 Phase 4 로 생긴 사용자 단계

- **`vibe-dotfiles` 레포에 미커밋 변경 발생** — `~/.config/vibe-tools` 는 `~/Project/vibe-dotfiles/vibe-tools` 심링크고 그 안 심링크가 git 추적 대상이다. `M` 6건 재타깃 + `??` 4건(원래 미추적). **커밋 판단은 사용자.**
- **새 셸부터** `vibe`·`vhelp`·`claude-delegate` alias 가 새 경로로 해석된다. 즉시 적용은 `source <레포>/claude/plugins/tmux-suite/aliases.zsh`.
- **기존 버그 1건 발견·미수정**: `tmux-suite/install.sh` 전체 모드가 `~/.zshrc` 에 실행마다 빈 줄 누적(awk 가 마커 앞 개행 미제거).

2026-09-13 사고 수습으로 **`vibe-ai-config` 의 프로젝트 Stop 훅을 꺼둔 상태다.**
- `vibe-ai-config/.claude/settings.json` ← `.claude/settings.json.phase1-bak`
- `vibe-ai-config/.codex/hooks.json` ← `.codex/hooks.json.phase1-bak`

복원 전에 3-D 를 먼저 처리할 것 — 그러지 않으면 같은 사고가 반복된다. 작업 중 이 레포에 에이전트 pane 을 띄우지 말고, 확인은 para cwd 에서 `git -C` 로 한다.

## 8. 사용자 단계 (에이전트 수행 불가)

1. Phase 1-B·2-B·3-B·3-F·5-B 등 **`hooks.json` 이 바뀌는 모든 단계 뒤** Codex TUI `/hooks` `Active` 열 확인 → 미신뢰 `t`. 추정 복원·자동 승인 금지.
2. `vibe-ai-config` 커밋·푸시 판단(미푸시 `13d561f`·`c99a80d` + 프로필 수정 미커밋). Phase 4-A/5 캐시 재동기화는 푸시 없이는 반영 안 됨.
3. 유령 플러그인 제거(6-B) — `claude plugin uninstall harness test vibe-admin`(명령 실측 필요).
4. 업무컴 `git pull` → `./install.sh work` → `diff status/personal.md status/work.md` · `dist/stdio.mjs` 빌드 대조.
5. §9 결정.

## 9. 결정 필요 목록

| # | 질문 | 권고 |
|---|---|---|
| 1 | 3-A: chain-guard(전부 deny) vs readonly-allow(읽기 파이프 허용) | chain-guard 정본, allow 훅 파이프 패턴 제거 |
| 2 | 4-2: 플러그인 A'(스크립트만 shared) / A(통째 이동) / B | **A'** — 실소비가 레포 직결이라 포장 이동은 비용만 크다 |
| 3 | 루트 `skills/` 명문화 vs 실이동 | 명문화 |
| 4 | `settings.base.json` permissions 승격 | 보류 |
| 5 | 3-C: analyze 19개 훅 — 발화 실측 후 수정 vs 실사용 스택만 남기고 축소 | 실측 → Kotlin·Python·PHP 만 존치 |
| 6 | 6-C: `bash-guard.sh` 회수 범위 | 시크릿·파괴명령 6규칙만, tmux 강제·worktree 차단 제외 |
| 7 | `marketplace.sh`·`engines/slack-bridge/`·`google-workspace` 플러그인 생존 | 사용자 확인 후 `_unused` 경유 없이 삭제(거버넌스 1줄과 함께) |
| 8 | 5-A: Codex 커맨드 미변환 7개 — 원인 규명에 시간 쓸지, `AGENTS.tail.md` 매핑 표로 우회할지 | 우회 먼저(스킬은 전부 도달 가능), 규명은 Codex 릴리즈 노트 확인 수준 |

## 10. 실행 순서 요약

```
Phase 1  1-E(gitignore, 즉시) → 1-A → 1-B → 1-C → 1-D → 1-F → 4-1(디커플링·references→shared)
         한 세션 · install.sh personal 1회 · 사용자 /hooks t
Phase 3  3-B → 3-F → 3-E (실동작 결함, 각 30분) · 3-C 실측 → 결정 5 · 3-D 원인 추적 · 3-A 결정 1 후
Phase 2  2-A → 2-C → 2-B → 2-D            test.sh/smoke/drift 3종 무출력이 완료 기준
Phase 5  5-B 규약 명문화 → 5-A 매핑 표·frontmatter → 5-C·5-D 문서화
Phase 6  6-C 회수(asset-intake 게이트) → 6-A 사문 삭제 → 6-B 유령 청소(사용자) → _unused 제거·거버넌스 1줄
Phase 4  4-2 A' (결정 2 후, 별도 세션) — 심링크 재타깃 · tmux-suite/install.sh · 경로 6곳
```

공통 VERIFY: `bash -n install.sh` · `shellcheck install.sh`(기존 SC2155 1건 외 신규 0) · `jq . <json>` · `./install.sh personal` ❌ 0 · 2회차 `diff` 무변경(멱등) · `git status` 의도 외 변경 없음 · `hooks.json` 변경 시 Codex `/hooks` Active 재확인.
