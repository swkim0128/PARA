# GEMINI.md — Gemini CLI / Antigravity 전용 지침

공통 규칙은 아래 정본에서 가져옵니다. **공통 내용은 이 파일이 아니라 `AGENTS.md` 를 수정하세요.**

@AGENTS.md

---

## 도구별 참고

- **Antigravity(agy)** 는 `AGENTS.md` 를 네이티브로 읽으며, 충돌 시 `GEMINI.md` 가 우선한다 — 이 파일에는 공통 규칙을 중복 기재하지 않는다.
- **Gemini CLI** 는 `GEMINI.md` 만 네이티브로 읽으므로 위 `@AGENTS.md` 임포트가 정본 연결 경로다.
- 이 볼트에서 agy 는 허브(메인 구현·오케스트레이션), Gemini CLI 는 조사·대량 컨텍스트 분석·문서화를 담당한다 (분업 상세는 정본 참조).
